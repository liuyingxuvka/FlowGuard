interface:
  display_name: "FlowGuard TestMesh"
  short_description: "Split large or slow validation evidence"
  default_prompt: "Use $flowguard-test-mesh when tests, checks, transition cells, or artifact payload case evidence need parent and child structure, freshness, or background completion proof."
