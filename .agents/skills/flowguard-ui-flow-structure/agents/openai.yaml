interface:
  display_name: "FlowGuard UI Flow"
  short_description: "Model UI flows, visible surface, and human-operability"
  default_prompt: "Use $flowguard-ui-flow-structure to model UI interactions first, review user-visible surface and evidence kinds, require reachable enabled controls to be clicked or scoped, add task coverage and human-operability evidence, derive stable menu/region/control hierarchy, then produce a UI text hierarchy blueprint with calm visual handoff guidance."
