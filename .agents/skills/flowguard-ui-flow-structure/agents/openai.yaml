interface:
  display_name: "FlowGuard UI Flow"
  short_description: "Model UI flows before deriving structure"
  default_prompt: "Use $flowguard-ui-flow-structure to model UI interactions first, then derive stable menu, region, and control hierarchy."
