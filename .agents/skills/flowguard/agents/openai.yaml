interface:
  display_name: "FlowGuard Model-First Kernel"
  short_description: "Route unclear work and build executable state models"
  default_prompt: "Use $flowguard for route_id=model_first_function_flow, role=kernel, native_owner=model_first_function_flow only after the compact route index finds no clear satellite owner. Extract evidenced task facts; zero or multiple owners stay blocked, and exactly one owner loads only its first protocol. A narrow entry is not a shallow mode: broad claims, predictions, model misses, ambiguity, high-impact gaps, and addressable native gaps trigger the mapped reference and model-predict-validate-revise work until native closure or an explicit external/scope/stall/limit terminal. Require the real engine, Input x State modeling, current authority, known-bad proof, and honest non-pass states; never create a fake or parallel controller. Compile current typed native-owner coverage in Model Maturation; never self-rate understanding. If code is requested, preserve that sufficiency result and obtain a separate DevelopmentProcessFlow implementation admission. Output evidence, failures, blockers, skipped_checks, residual_risk, claim_boundary, and typed_next_actions. For a concrete model change, freeze task-specific one-or-many protected failures and bind native good/bad-per-failure/oracle/current evidence; reusable model types are not permanently single-purpose and only declared checks may support completion claims."
