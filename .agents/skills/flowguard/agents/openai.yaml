interface:
  display_name: "FlowGuard Model-First Kernel"
  short_description: "Route unclear work and build executable state models"
  default_prompt: "Use $flowguard for route_id=model_first_function_flow, role=kernel, native_owner=model_first_function_flow only after the compact route index finds no clear satellite owner. Freeze evidenced TaskFacts, compile the non-reducible TaskCoverageDemand, and show each owner as satisfied|not_triggered|unresolved|blocked; ordinary|standard|deep|release is derived presentation depth, not a user-selectable weak correctness mode. Zero or multiple route owners stay visible. Declare one-or-many protected failures; reusable model types are not permanently single-purpose, and only declared checks may support completion claims. Build the smallest real Input x State model for triggered owners, with current authority and task-specific native good, bad-per-failure, oracle, and evidence. Mature unresolved rows to a genuine terminal, publish a canonical EvidenceReceipt, and accept sufficiency only through independent exact verification as VerifiedModelMaturation; never trust raw mappings, self-rating, current flags, progress, stale, skipped, or not-run evidence. If code is requested, preserve maturation and obtain separate DevelopmentProcessFlow admission; permission cannot upgrade understanding. RiskLedger alone owns confidence and Closure checks only exact identity/material/terminal integrity. Target-product roles stay in the target model. Output evidence, failures, blockers, skipped_checks, residual_risk, claim_boundary, and typed_next_actions."
