# DevelopmentProcessFlow Handoff

This kernel-side file is a compact handoff stub. The detailed protocol is
owned by the on-demand `development-process` domain reference.

Load:
`.agents/skills/flowguard/references/domains/development-process-flow/references/development_process_flow_protocol.md`

Use this domain when staged plan/edit/test/fix/verify work, artifact versions,
peer writes, evidence freshness, minimum revalidation, install or shadow sync,
or done/release/archive/publish confidence is the risky boundary.

Keep the hard gates: lifecycle evidence does not replace sibling route
evidence, running background checks are not pass evidence, and scoped or stale
validation must stay visible until revalidated.
