## 1. Ownership Model

- [x] 1.1 Extend the direct latest-schema policy and project-adoption models with exact bounded artifact ownership, target-owned write accounting, and numeric-schema/legacy-lookalike known-bad variants.
- [x] 1.2 Add model scenarios and invariants that reject target-owned JSON mutation while accepting the exact historical FlowGuard BCL migration at both public entrypoints.

## 2. Artifact Upgrade Root Fix

- [x] 2.1 Replace numeric-schema, prefix, and producer-label ownership inference with one exact in-package JSON migration registry.
- [x] 2.2 Keep only the exact `56083c1e` bare behavior-ledger producer shape on its evidence-bound direct-to-current canonical writer; keep all BCL envelopes and report/trace registrations current-only.
- [x] 2.3 Ensure unregistered JSON returns outside the upgrade result without serialization or any path-specific exclusion.

## 3. Executable Regression

- [x] 3.1 Add artifact-upgrade cases for integer/numeric-string schema fields, unknown FlowGuard-looking types, producer labels, envelope-like payloads, report/trace symmetry, future BCL envelopes, and legacy-lookalikes with target-only fields.
- [x] 3.2 Add a real project-upgrade regression that proves a target-owned fixture remains byte- and SHA-256-identical while a typed historical FlowGuard behavior ledger migrates in the same run.
- [x] 3.3 Run the focused model, artifact-upgrade, project-adoption, CLI, and non-editable installation checks.
- [x] 3.4 Keep the bundled behavior-ledger template on the exact current canonical producer shape and bind it with execution plus serializer round-trip regression.

## 4. Maintainer Synchronization

- [x] 4.1 Update versioned project integration, changelog, release checklist, package metadata, and visible version records to 0.58.4.
- [x] 4.2 Recompile the affected SkillGuard contract, clean consumer releases, and package-owned 15-skill authority after source edits freeze.
- [x] 4.3 Run strict OpenSpec validation, all affected model/test owners, installation/currentness checks, and one clean foreground final full gate.

## 5. Release Closure

- [x] 5.1 Commit the frozen source, publish annotated tag and source-only GitHub Release v0.58.4 with zero assets, and require CI success on the exact commit.
- [x] 5.2 Pass the published verifier and confirm source, install, Git, version, tag, Release, and global 15-skill projection parity.
- [x] 5.3 Hand the exact immutable v0.58.4 identity to Khaos and record the model-miss lesson through KB postflight.
