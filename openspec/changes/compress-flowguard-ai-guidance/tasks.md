## 1. Prompt Surface Compression

- [x] 1.1 Add prompt-budget tests for kernel, satellite `SKILL.md`, and AGENTS snippet hot paths.
- [x] 1.2 Compress `model-first-function-flow/SKILL.md` into a thin router that delegates protocol detail to references.
- [x] 1.3 Compress each FlowGuard satellite `SKILL.md` into a concise route shell while preserving standalone hard gates.
- [x] 1.4 Compress `docs/agents_snippet.md` into a compact adoption/router snippet with reference handoffs.

## 2. FlowGuard Evidence And Regression

- [x] 2.1 Add or update a FlowGuard model/check that covers hot-path compression, installed skill sync, shadow workspace sync, and git evidence.
- [x] 2.2 Run OpenSpec status/validation and focused skill documentation tests.
- [x] 2.3 Run practical FlowGuard regression checks for the touched guidance and model surfaces.

## 3. Local Version And Sync

- [x] 3.1 Update local package/documentation version records and changelog for the guidance-compression release.
- [x] 3.2 Refresh the editable local install from the git checkout and verify package version, schema, and import path.
- [x] 3.3 Synchronize repository-managed skills into the installed Codex skills directory and verify representative content.
- [x] 3.4 Synchronize the git checkout back to the shadow workspace and verify shadow import behavior.

## 4. Finalization

- [x] 4.1 Record FlowGuard adoption evidence for trigger, model/checks, validations, sync results, and skipped gaps.
- [x] 4.2 Create a scoped local git commit and local tag after validation passes.
- [x] 4.3 Run KB postflight and record any reusable maintenance lesson.
