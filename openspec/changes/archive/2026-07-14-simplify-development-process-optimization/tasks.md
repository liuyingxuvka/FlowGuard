## 1. Confirm the archived prerequisite and freeze ownership

- [x] 1.1 Complete the remaining runtime/contract reconciliation in `reconcile-spec-provider-work-packages-with-flowguard-evidence` without changing this optimizer schema.
- [x] 1.2 Refresh only the stale native-skill child receipts required by the reconcile change and re-aggregate self-governance evidence.
- [x] 1.3 Freeze the reconcile change's source and validation plan, run its single owned full-model and full-pytest checks, and close its same-session evidence.
- [x] 1.4 Verify and archive the reconcile change, then confirm no active change or peer owns the optimizer, DPF, TestMesh, PlanDetail, MTA, API, or DPF-skill files.
- [x] 1.5 Revalidate this change's OpenSpec artifacts against the newly canonical specs and record the exact affected component inventory.
- [x] 1.6 Compile the current OpenSpec 1.6 native verification plan, with only `focused|full` scopes, no nested session wrappers, and exactly one execution owner for every entry in `verification-contract.yaml`.

## 2. Preserve the behavior contract before contraction

- [x] 2.1 Update the existing development-process Behavior Commitment in place, preserving its intent id, commitment id, plane, primary owner, and public DPF route.
- [x] 2.2 Replace the strategy child model with conditional activation, six-axis hard equivalence, diagnostic-boundary/execution-mode composition, repair groups, affected revalidation, and material-evidence freshness.
- [x] 2.3 Update ContractExhaustion cases for inactive ordinary work, correlated diagnostics, hard blockers, budgeted stops, unsafe parallelism, primary-owner repair, stale decisions, and incomplete revalidation.
- [x] 2.4 Rewrite the FieldLifecycle inventory as direct current replacement, correct the real TestMesh/Finding Ledger/PlanDetail source paths, and give every retired field a zero-authority disposition.
- [x] 2.5 Update Architecture Reduction and code-structure evidence with the observable contract, retained hard gates, deletion inventory, target ownership, and no-new-route decision.
- [x] 2.6 Update the model registry and API/commitment source evidence so every affected model and real `flowguard/__init__.py` surface has one current owner.

## 3. Perform one atomic runtime schema cutover

- [x] 3.1 Replace `development_process_strategy.py` with at most five inspectable dataclasses and `review_process_optimization`, preserving all six hard-equivalence dimensions, dependency validity, isolation, bounded claims, and repair closure.
- [x] 3.2 Remove public numeric cost vectors, Pareto/frontier logic, six-policy constants, rollout stages, campaign/cluster/hypothesis/batch/reevaluation graphs, synthetic projections, and the former standalone review/API group.
- [x] 3.3 Change DevelopmentProcessFlow plans to only `process_optimization_reasons` and `required_process_optimization_evidence_ids`, consume typed current evidence, derive `not_needed|selected|blocked`, and retain coverage-aware affected revalidation.
- [x] 3.4 Remove all strategy-specific fields from DPF actions and validation rows, using their ordinary evidence, ordering, repair, and validation-requirement fields instead.
- [x] 3.5 Change the simulator to one reason-id collection plus one evidence-id collection while retaining the internal `strategy_selection` mode id and mode ordering.
- [x] 3.6 Make TestMesh own only diagnostic campaign id, boundary, planned/executed/failed/not-run counts, visible not-run reason, and stable finding ids; enforce the count and completeness invariants.
- [x] 3.7 Change PlanDetail to project only plan-level optimization reason/evidence ids and delete every strategy-specific step and validation projection.
- [x] 3.8 Delete `ProcessStrategyAlignmentBinding` and its MTA plan/coercion/finding/API surfaces; close repair groups through ordinary affected-obligation, primary-owner, and test-evidence records.
- [x] 3.9 Update summary/report projection to expose bounded DPF optimizer status and gaps without recreating a parallel strategy report.
- [x] 3.10 Update `flowguard/__init__.py` last, exposing no more than five optimizer records plus one review under the existing DPF API group and asserting all retired names are absent.

## 4. Replace score tests with behavior and trajectory evidence

- [x] 4.1 Rewrite optimizer unit tests for six-axis equivalence, candidate eligibility, qualitative/measured comparison boundaries, dependency cycles, safe-parallel isolation, and bounded claim wording.
- [x] 4.2 Replace the caller-authored score benchmark with deterministic replay traces that first prove equal terminal result/evidence/safety/authority and then compare check, repair, revalidation, handoff, and not-run events.
- [x] 4.3 Add traces for repeated fail-fix versus boundary-first repair, hard-blocker stop, safe parallel shards, budgeted stop, material-change replan, ordinary inactive work, and one non-test workflow.
- [x] 4.4 Update DPF and simulator tests for reason/evidence admission, inactive zero-ceremony output, stale evidence, blocked claims, and removal of rollout/adaptive policy state.
- [x] 4.5 Update TestMesh, PlanDetail, MTA, summary, Behavior Ledger, FieldLifecycle, ContractExhaustion, and API tests for their contracted owner boundaries.
- [x] 4.6 Add `tests/test_process_optimization_zero_residuals.py` to enforce dataclass/symbol/line budgets and zero former symbols, fields, values, finding codes, aliases, wrappers, and dual readers outside explicit history/negative fixtures.

## 5. Contract the agent guidance under SkillGuard supervision

- [x] 5.1 Update the DPF skill route shell so process optimization activates only for the four stable reason classes and ordinary work has no candidate-selection ceremony.
- [x] 5.2 Reduce the core DPF lifecycle protocol and move only active optimizer detail and related-failure triage into two demand-loaded references.
- [x] 5.3 Preserve the original general rule in guidance: gather additional valid related evidence before repair when useful, stop when a hard blocker invalidates descendants, repair evidence-related findings together, and revalidate every affected obligation.
- [x] 5.4 Update the OpenAI prompt, project-adoption source, generated AGENTS projection, templates, README/docs, and examples to the same conditional trigger, two-dimension vocabulary, and bounded claim language.
- [x] 5.5 Update the DPF SkillGuard contract source and exact check manifest, then compile the current contract with no compatibility reader, alternate manifest, fallback, or migration path.
- [x] 5.6 Update skill prompt/parity/inventory/docs tests and prove retired six-policy/rollout language has zero current-success residuals.

## 6. Run affected-only diagnostics and close every finding

- [x] 6.1 Run OpenSpec strict validation, model-manifest audit, SkillGuard contract checks, and the zero-residual test as independent focused diagnostics so one failure does not hide the rest.
- [x] 6.2 Run affected FlowGuard models for strategy, DPF, TestMesh, PlanDetail, BCL, FieldLifecycle, MTA, and architecture ownership with safe parallel jobs and separate receipts.
- [x] 6.3 Run optimizer/DPF/simulator tests, trajectory traces, owner-integration tests, and API/prompt/template tests in disjoint affected groups.
- [x] 6.4 Run the DPF native skill check, targeted static check, and frozen-root-aware SkillGuard project audit under their declared owners; do not force unrelated skills to rerun.
- [x] 6.5 Preserve every failure as a stable finding, group only evidence-related findings, implement root-cause repairs, and rerun only the invalidated affected owners.
- [x] 6.6 Confirm all affected checks are terminal-success, every not-run item has a reason, every repair group has current owner/revalidation evidence, and no unresolved obligation remains.

## 7. Promote one frozen current implementation

- [x] 7.1 Freeze the exact promotion inventory and run a shadow-to-formal dry run without publication, tagging, or remote push.
- [x] 7.2 Promote the exact current source, models, tests, specs, scripts, skills, prompts, templates, and docs while preserving the formal worktree's existing unrelated changes.
- [x] 7.3 Delete every planned retired formal path/field/symbol that copy-only synchronization cannot remove, and rerun the formal zero-residual and affected gates.
- [x] 7.4 Install the package editably from the formal repository and verify version `0.55.0`, schema `1.0`, import path, and current optimizer API.
- [x] 7.5 Install the complete managed skill suite from the formal repository once, then prove formal/shadow/installed skill parity and current DPF native depth.
- [x] 7.6 Sync the formal snapshot back to the shadow workspace, verify exact governed-file parity, and freeze source, toolchain, check plan, installed projection, and execution identity.

## 8. Execute one final owner and archive only current proof

- [x] 8.1 Run `openspec verify simplify-development-process-optimization --mode full --json` once for the final corrected frozen snapshot; let `check.models.full` and `check.tests.full` be the only full-model and full-pytest execution owners, and do not reuse the superseded pre-archive snapshot.
- [x] 8.2 If the final launcher fails or is interrupted, confirm the entire descendant process tree is zero before repairing and starting a new execution identity; never resume as a read-only audit or use an unattended/background retry.
- [x] 8.3 Inspect every immutable receipt, frozen snapshot manifest, post-run input-stability result, input/toolchain identity, coverage mapping, installed parity result, and skipped/not-run fact; reject stale, partial, duplicate-owner, or cleanup-unconfirmed evidence.
- [x] 8.4 Update FlowGuard machine/human adoption logs with trigger, elapsed time, model files, commands, findings, counterexamples, skipped steps, friction, and exact claim boundary.
- [x] 8.5 Perform the predictive-KB postflight; record a structured candidate only for a real reusable route/skill/delegation/process lesson and keep trusted-card maintenance out of this task.
- [x] 8.6 Mark tasks complete only from the final pass report and unchanged frozen inputs, then archive directly; do not overwrite the pass report with a later `--no-run` report.
- [x] 8.7 Confirm the formal repository, shadow workspace, installed package, installed skills, canonical OpenSpec specs, FlowGuard models, and user-facing guidance all represent the same current implementation with no obvious next action left.
