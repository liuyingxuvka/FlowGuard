## 1. Route Graph And Self-Maintenance Model

- [x] 1.1 Add a FlowGuard self-maintenance model/check artifact that represents route graph, field layering, structure, validation, install sync, and closure child reports.
- [x] 1.2 Expand the package route graph so installed FlowGuard routes have compact discovery groups with triggers, minimal inputs, outputs, evidence owners, and next actions.
- [x] 1.3 Add route graph completeness checks that fail when installed route helpers/templates/skills are missing route discovery groups.

## 2. AI Entry Profiles And Field Layering

- [x] 2.1 Add lightweight AI entry profiles for common self-maintenance tasks so route-first usage does not start from the flat helper list.
- [x] 2.2 Add shared field/evidence layer helpers for core fields, route-owned fields, shared proof fields, metadata/display fields, and compatibility disposition.
- [x] 2.3 Add tests proving field profiles preserve full route expansion and do not delete behavior-bearing evidence.

## 3. Structure And Public API Governance

- [x] 3.1 Preserve public facades while adding route-first API grouping tests for newly connected routes.
- [x] 3.2 Add a focused StructureMesh-style report or model row for oversized route modules that should be split later behind stable public entrypoints.
- [x] 3.3 Update public API/docs so AI consumers see route profiles before broad helper lists.

## 4. Skill, Template, And Documentation Sync

- [x] 4.1 Update installed FlowGuard skill route maps and compact guidance to include the self-maintenance chain.
- [x] 4.2 Update template/API docs to show SummaryReport -> MaintenanceScan -> route owner -> evidence -> closure flow.
- [x] 4.3 Update OpenSpec specs/tasks and adoption records with model files, evidence, skipped gaps, and next actions.

## 5. Validation, Install Sync, And Local State

- [x] 5.1 Run route-specific FlowGuard self-model checks and OpenSpec strict validation.
- [x] 5.2 Run focused unit tests for API surface, public templates, skill docs, project adoption, field lifecycle, model-test alignment, structure mesh, test mesh, and development process flow.
- [x] 5.3 Run practical regression in the background where useful and collect exit/status evidence before final confidence.
- [x] 5.4 Sync editable/local installed version and verify metadata version, import path, route feature availability, and installed skill visibility.
- [x] 5.5 Check local repository/git status and shadow workspace sync without reverting peer-agent changes; record any unavailable git boundary as scoped evidence.
