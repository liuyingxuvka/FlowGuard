## 1. Freeze the model-first change boundary

- [x] 1.1 Validate the complete OpenSpec proposal, design, delta specs, and task graph with strict checks.
- [x] 1.2 Update the current FlowGuard model-angle model so bare resolution, wrong-owner proof, stale proof, and missing angle coverage fail.
- [x] 1.3 Update the current DevelopmentProcessFlow model with independent sufficiency, authorization, admission, scope, invalidation, and non-waivable blocker states.
- [x] 1.4 Update current preflight/closure model handoffs so full preflight is not readiness and broad closure requires the same current maturation identity as risk.
- [x] 1.5 Run focused model checks and preserve representative known-good and known-bad evidence before production implementation.

## 2. Implement task-local sufficiency and implementation admission

- [x] 2.1 Bind ModelAngle `resolved` observations to exact current owner `ProofArtifactRef` evidence and subject fingerprints; project open angles to maturation.
- [x] 2.2 Add current-schema `ModelMaturationCoverageContribution` and `ModelMaturationIntake` contracts plus a deterministic compiler into the existing maturation plan.
- [x] 2.3 Extend maturation reports with exact candidate/coverage/evidence identities and add a compact typed maturation evidence reference for downstream consumers.
- [x] 2.4 Add DevelopmentProcessFlow implementation authorization/admission plans and reports with ready, ready-scoped, no-code, blocked, and stale outcomes.
- [x] 2.5 Add the typed Model Maturation risk gate and exact gate adapter/findings to Risk Evidence Ledger.
- [x] 2.6 Require Closure Contract to consume exact current maturation evidence and the same maturation identity used by Risk Evidence Ledger.
- [x] 2.7 Bind Code Structure Recommendation implementation-ready claims to the exact current admitted task/model/coverage/artifact scope.
- [x] 2.8 Export the new current public helper types, keep CLI/API registry identity current, and update current templates without adding a public route or understanding CLI.

## 3. Connect native owners and agent guidance

- [x] 3.1 Update the FlowGuard kernel SKILL, OpenAI prompt, skill-kernel protocol, and modeling-evidence protocol with the preflight-to-maturation-to-admission chain and two-result output.
- [x] 3.2 Update ExistingModelPreflight guidance so full means owner-map completeness only, owner proof is required for angle resolution, and typed coverage is handed to maturation.
- [x] 3.3 Update DevelopmentProcessFlow and PlanDetail guidance with internal implementation admission, exact bounded authorization, non-waivable blockers, and no-code handling.
- [x] 3.4 Update Code Structure Recommendation guidance so early recommendations remain recommendation-only until exact admission.
- [x] 3.5 Update BCL, FieldLifecycleMesh, UI Flow Structure, and TestMesh protocols/prompts with typed maturation contributions while preserving target-domain roles and native semantics.
- [x] 3.6 Keep ModelMesh and Model-Test Alignment existing maturation ownership intact and verify no duplicate public route or semantic owner is introduced.

## 4. Prove runtime, prompt, and template behavior

- [x] 4.1 Add focused ModelAngle tests for missing, wrong-owner, stale, wrong-obligation, wrong-fingerprint, and exact current proof.
- [x] 4.2 Add maturation compiler tests for independent denominator union, omitted BCL/angle/UI/field/test items, stale contributions, duplicates, low-risk narrowing, and self-report rejection.
- [x] 4.3 Add admission tests for no-code, normal ready, blocked open gaps, exact ready-scoped override, scope expansion, stale fingerprints, conflicting ownership, and read-only blockers.
- [x] 4.4 Add Risk/Closure tests for missing, stale, scoped, blocked, mismatched, and exact shared maturation evidence.
- [x] 4.5 Add Code Structure tests for recommendation-only output, exact admitted readiness, and scoped-admission expansion blocking.
- [x] 4.6 Update templates, CLI/API/public-surface tests, route topology tests, prompt budget tests, and source/install prompt parity expectations.
- [x] 4.7 Run focused affected tests, then the complete ordinary test suite, and repair every current failure without rolling back peer work.

## 5. Reconcile model authority and OpenSpec lifecycle

- [x] 5.1 Freeze the affected multi-model closure and run exactly one final model-regression parent owner after model/source inputs are stable.
- [x] 5.2 Build and atomically activate one current ModelRevisionSet against the exact observed head; verify the new observed snapshot and affected siblings.
- [x] 5.3 Verify implementation completeness, correctness, and design coherence against every OpenSpec requirement and scenario.
- [x] 5.4 Sync all delta specs to the main specs and archive the completed OpenSpec change with every implementation task complete.

## Post-archive maintenance and release handoff

After this change is archived, the repository-level execution plan continues
with the already authorized SkillGuard contract refresh, frozen maintenance-unit
validation, transactional consumer installation, source/install/shadow parity,
patch-version release gate, scoped Git commit, `v0.68.3` tag, GitHub Release,
remote parity verification, and predictive-KB postflight. These post-archive
delivery actions are not implementation prerequisites of this OpenSpec change.
