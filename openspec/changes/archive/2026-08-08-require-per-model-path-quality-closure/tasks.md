## 1. Canonical path-quality kernel

- [x] 1.1 Extend the existing `model_maturation_loop` executable self model with required per-model path-quality summaries, current subject fingerprints, blocked unresolved cases, and a known-bad bypass workflow before production implementation.
- [x] 1.2 Add the five internal immutable records `PathQualitySubject`, `PathCostVector`, `NecessityWitness`, `PathCandidate`, and `PathQualityResult`, canonical validation, deterministic JSON projection, and content fingerprints without adding a public route, command, skill, or second current pointer.
- [x] 1.3 Implement the deterministic lightweight review for unreachable states/transitions, duplicate guards/effects, behavior-irrelevant state, pass-through FunctionBlocks, unconsumed outputs, repeated identical validation, duplicate exact-current ownership, and no-progress loops with indexed near-linear traversal.
- [x] 1.4 Implement exact deep-review triggers so ordinary current `single_clear_path` work creates no candidate expansion or deep payload, while explicit, structural, miss-derived, missing-witness, high-cost, and release-critical triggers remain visible and affected-model scoped.
- [x] 1.5 Implement finite candidate and rewrite-set validation, including hard-semantic comparison across inputs, outputs, state/fields, errors, effects, order, retry, timeout, progress/fairness, permissions, parent/child interfaces, intent, authority, oracles, and evidence obligations.
- [x] 1.6 Implement dominance-aware cost-vector comparison without a default scalar sum and license only `single_clear_path`, `preferred_within_candidates`, `non_dominated_within_boundary`, `minimum_within_exhausted_finite_set`, `locally_irreducible_under_declared_rewrites`, or `unresolved`.
- [x] 1.7 Implement retained-element necessity-witness validation, reject missing/duplicate/stale/circular/self-licensed witnesses, and bind accepted witnesses to exact obligations, counterexamples, oracles, and evidence identities.
- [x] 1.8 Add focused tests for valid/invalid records, deterministic fingerprints, ordinary compact output, every lightweight finding, every deep trigger, semantic mismatch, Pareto incomparability, finite-set minimum, local irreducibility, ties, missing measurements, missing witnesses, stale identities, and direct rejection of global-optimum language.

## 2. ModelMaturation and executable consumption

- [x] 2.1 Integrate required model ids and current path-quality results into ModelMaturation intake, coverage compilation, iteration progress, decision, report, and canonical maturation receipt without turning self-report into evidence.
- [x] 2.2 Integrate compact result intake/output with the existing plan, runner, portable model, and portable checker paths so provider-neutral targets can supply normalized model facts and receive the same bounded conclusions.
- [x] 2.3 Keep observed and normative lanes separate: an equivalent representation rewrite may update a model revision, while any behavior-changing candidate remains `normative_target` until code, bindings, evidence, and observed activation match.
- [x] 2.4 Add unit, CLI/portable, receipt, and native `model_maturation_loop` cases proving that missing, stale, semantically mismatched, or unresolved required path-quality rows block only the matching maturation/activation claim and cannot be upgraded by permission.
- [x] 2.5 Run the updated native maturation model and affected ModelMaturation tests; inspect every counterexample and revise the design before continuing if a hard invariant fails.

## 3. Blueprint, topology, evidence, and v5 authority

- [x] 3.1 Add provider-neutral path-quality subject and compact-summary fields to target/software blueprint records and canonical projections without requiring Python-specific source fields or duplicating deep evidence in parent blocks.
- [x] 3.2 Extend software-blueprint readiness and affected compact readers to require exact current results for new/materially changed denominator members while reusing unchanged current rows only through exact affected-topology evidence.
- [x] 3.3 Extend ModelMesh parent/child and affected-sibling propagation with compact path-quality subject/currentness/detail fingerprints; reopen topology-required consumers when child identity changes and keep single-model judgment with ModelMaturation.
- [x] 3.4 Extend Model-Test Alignment and TestMesh bindings so every affected hard semantic obligation and necessity witness maps to one owner code contract, exact test member, executable oracle, current result, or explicit scoped disposition without duplicating optimality judgment.
- [x] 3.5 Complete the coordinated v5 cumulative current-intent and strict authority implementation, then add path-quality summaries to the same ModelRevisionSet candidate identity, native-owner evidence plan, compare-and-swap activation, and transition receipt with no independent authority file.
- [x] 3.6 Add referential-integrity, stale-subject, changed-provider/dependency/code/test/oracle/evidence, parent-summary, partial-candidate, CAS-conflict, legacy-rejection, and observed-versus-target authority tests.
- [x] 3.7 Replace exact equality between the add-or-replace set and the candidate path-quality result set with minimum changed-member coverage plus strict validation of every optional current-model row; add regressions for a 5-model increment carrying all 51 current self-DNA rows and for foreign, stale, cross-snapshot, unvalidated, or unresolved extras.
- [x] 3.8 Keep ordinary revision admission affected-only, but require the exact complete current-model path-quality denominator when FlowGuard explicitly performs whole-self qualification; add passing 51/51 and blocked affected-only native scenarios.

## 4. Process, kernel, skills, and token boundary

- [x] 4.1 Update DevelopmentProcessFlow so owner/intent closure precedes lightweight path review, triggered deep review precedes its affected behavior-sensitive implementation claim, implementation changes minimally stale affected results, and final activation consumes the exact current summary.
- [x] 4.2 Keep model-path quality distinct from DPF work-order optimization: replace any unexplained scalar process scoring with its own bounded comparable process evidence while preventing either subsystem from claiming the other's optimum.
- [x] 4.3 Update the FlowGuard kernel and affected ModelMaturation, ExistingModelPreflight, ModelMesh, ArchitectureReduction, Model-Test Alignment, TestMesh, and DevelopmentProcessFlow author prompts/protocols to expose the compact result, conditional deep triggers, owner boundaries, and non-global language without adding a route or reconstruction instruction.
- [x] 4.4 Add prompt and documentation regressions proving ordinary use does not load deep candidates, reconstruction is never implied, non-Python/non-code targets remain supported, and unresolved rows stay visible at the correct claim boundary.
- [x] 4.5 Update SkillGuard source contracts and generated author metadata only after skill text is frozen; run affected-member author checks and preserve direct-current consumer projection with zero aliases, compatibility readers, or author receipt leakage.

## 5. FlowGuard self-use and contraction

- [x] 5.1 Freeze the final continuing self-model denominator after the coordinated historical/construction-owner retirement work and generate a current lightweight path-quality result for every continuing model rather than spending evidence on owners already proven for retirement.
- [x] 5.2 Run deep finite review only for self models triggered by duplicate/unreachable structure, repeated work, missing necessity witnesses, current model misses, explicit release-critical scope, or multiple hard-equivalent candidates; keep all other models compact.
- [x] 5.3 Apply every evidence-ready model-only rewrite, rerun its native model checks, and preserve faithful observed behavior; place intentional behavior improvements in normative targets until implementation and evidence close.
- [x] 5.4 Hand exact blueprint-mapped implementation candidates to Architecture Reduction and apply only independently proved code/helper/module/facade/validation contractions with consumer, side-effect, public-entrypoint, retirement, and affected-test closure.
- [x] 5.5 Measure before/after current-model count, state/transition/branch count, repeated operation count, validation duplication, source size, compact payload bytes, deep-detail bytes, and ordinary token-facing projection; separate immutable history from current runtime and avoid overclaiming loaded cost.
- [x] 5.6 Require zero dangling retired identities, duplicate current owners, unjustified retained elements, unresolved required path-quality rows, fallback paths, or unlicensed global-optimum claims before the whole-system DNA and release boundary can close.

## 6. Coordinated OpenSpec and pre-archive closure

- [x] 6.1 Run affected unit, native-model, API, CLI, portable, blueprint, readiness, topology, authority, alignment, process, skill, self-reduction, and token/payload checks after each stable integration batch and repair failures immediately.
- [x] 6.2 Synchronize the new delta specs plus all coordinated active-change deltas into current main specs, validate every change strictly, and add exact current intent contributions before pre-archive authority construction.
- [x] 6.3 Freeze the final source, toolchain, owner, model, intent, provider, dependency, test, and impact-plan identities and build one integrated pre-archive candidate ModelRevisionSet covering all coordinated changes rather than serially activating one candidate per change.
- [x] 6.4 Run one supervised pre-archive full model-regression parent and one compact FlowGuard self-blueprint, Architecture Reduction, topology, path-quality, and evidence audit over the same candidate identity; require every native owner obligation to retain its own evidence.
- [x] 6.5 Complete the remaining task and current-authority closure rows for `repair-per-behavior-block-binding`, `reattach-current-self-topology-evidence`, `bound-self-audit-materialization`, `bind-current-author-and-intent-projections`, `retire-redundant-historical-routes`, and this change without reusing stale archive-sensitive evidence.
- [x] 6.6 Verify that all coordinated OpenSpec changes are archive-ready, freeze the exact bulk-archive input and command, and hand post-archive authority renewal, final release validation, installation synchronization, Git publication, GitHub Release verification, and predictive-KB postflight to the top-level DevelopmentProcessFlow release plan. Those post-archive operations must not be owned by an artifact that moves during archive.

Post-archive closure remains mandatory, but its mutable execution checklist is
owned by the top-level DevelopmentProcessFlow release plan. This change ends at
truthful archive readiness so final validation never has to be invalidated merely
to edit a checklist that has already moved into the archive.
