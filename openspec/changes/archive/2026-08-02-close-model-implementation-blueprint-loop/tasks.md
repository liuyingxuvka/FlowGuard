## 1. Freeze Boundary and Failure Contracts

- [x] 1.1 Record the exact source, observed snapshot, semantic mesh, BCL inventory, current install, SkillGuard unit, Git, tag, and release identities plus every pre-existing parallel-work blocker.
- [x] 1.2 Remove fixed model/relation-count and caller-summary completion shortcuts from the self-understanding models and bind them to current verified semantic-mesh identities and exact set equality.
- [x] 1.3 Add pre-code ModelTestAlignment obligations and known-bad cases for omitted production files, parse failures, unsupported dynamic surfaces, orphan helpers, hidden state/effect writers, duplicate primary owners, path-only bindings, missing resources, stale fingerprints, tampered shards, and automatic reconstruction.
- [x] 1.4 Make every authoritative semantic-mesh/model input reachable from the clean release tree through tracked source or one deterministic tracked generation owner.

## 2. Independent Implementation Inventory

- [x] 2.1 Implement immutable software-boundary, implementation-surface, file-disposition, inventory, finding, and report types with canonical fingerprints and strict current-schema validation.
- [x] 2.2 Reuse the project validation file-manifest authority to build the exact tracked and admitted non-ignored boundary denominator without trusting model or contract declarations.
- [x] 2.3 Implement the Python AST discovery adapter for modules, classes, functions, methods, entrypoints, helpers, calls, reads, writes, and side-effect candidates while preserving parse/dynamic uncertainty as blockers.
- [x] 2.4 Require one terminal implementation disposition for every admitted file and symbol and reject unresolved files, unowned behavior-bearing surfaces, hidden writers/effects, path escape, missing adapters, and stale content fingerprints.
- [x] 2.5 Add deterministic inventory serialization/load helpers and a read-only inventory-audit CLI with zero filesystem writes or owner execution.

## 3. Bidirectional Binding and Reconstruction Semantics

- [x] 3.1 Implement immutable model-implementation binding, semantic-spec reference, oracle reference, artifact/resource reference, binding-report, and reconstruction-evidence types.
- [x] 3.2 Review exact model-required versus bound and implementation-required versus dispositioned sets, including duplicate primary owners, orphan helpers, hidden writers/effects, missing consumers, and stale model/source/contract fingerprints.
- [x] 3.3 Extend ModelTestAlignment to consume the independent binding report and expose implementation surfaces, bindings, inventory identity, semantic specifications, and oracles without turning every helper into a CodeContract.
- [x] 3.4 Extend CodeStructureRecommendation with an exact current model-element universe and reverse implementation-coverage obligations; reject nonempty but incomplete mappings.
- [x] 3.5 Extend StructureMesh with the independent inventory fingerprint and exact required partition-surface closure while keeping source parsing outside the route.

## 4. Cross-Layer Blueprint and Portable Projection

- [x] 4.1 Implement the software-blueprint manifest and qualification report over the current observed snapshot, implementation inventory, binding report, model topology, portable identities, build/runtime/dependency/config/schema/data/asset/migration/service resources, and oracles.
- [x] 4.2 Keep static status (`complete|incomplete|stale|blocked`) independent from empirical reconstruction status (`not_run|pass|fail|blocked`) and require exact receipt identity only when reconstruction is explicitly required.
- [x] 4.3 Implement deterministic canonical manifest and content-addressed shard export with path containment, missing-reference, tamper, and re-export identity verification; exclude production source text by default.
- [x] 4.4 Add read-only blueprint-check and explicit blueprint-export CLI surfaces with canonical JSON, stable exit semantics, no missing-owner execution, no authority mutation, and no automatic reconstruction.
- [x] 4.5 Register one implementation-blueprint API cohort under `model_first_function_flow` and verify import, registry, serialization, and CLI parity without adding a route or skill.
- [x] 4.6 Add affected-neighborhood and content-addressed shard reuse so ordinary work does not load, scan, export, or reconstruct the whole repository.

## 5. FlowGuard Self Blueprint and Model Authority

- [x] 5.1 Create the tracked FlowGuard software-boundary and disposition definition covering production source, skills, build/config/schema/data/assets/migrations/external services, tests, and explicit exclusions.
- [x] 5.2 Build FlowGuard's current implementation inventory, bind every behavior-bearing surface and helper disposition to existing model/contract owners, and close build/data/resource/oracle references.
- [x] 5.3 Add the executable implementation-blueprint model and runner with current good cases, one bad case per protected failure, scenario/loop/progress/contract checks, and model-regression-manifest ownership.
- [x] 5.4 Connect the blueprint qualification to the current semantic mesh as a derived consumer without adding another observed authority head or changing PortableModel/PortableSystem v1 wire schemas.
- [x] 5.5 Run the exact whole-FlowGuard self-understanding chain over the expanded current model universe, publish and independently verify the maturation receipt, obtain admission/risk/closure, and accept one current ModelRevisionSet only after every consumer is green.

## 6. Skills, Contracts, Documentation, and Installation Inputs

- [x] 6.1 Update the FlowGuard kernel and modeling protocols so explicit whole-software blueprint facts trigger current owners, ordinary work stays affected-only, static and empirical claims remain separate, and reconstruction never runs automatically.
- [x] 6.2 Update ExistingModelPreflight, CodeStructureRecommendation, StructureMesh, ModelTestAlignment, ModelMesh, and DevelopmentProcessFlow skill prompts/references with their exact blueprint handoffs and unchanged ownership boundaries.
- [x] 6.3 Keep BCL external-only and document conditional FieldLifecycleMesh, UI Flow Structure, TestMesh, ArchitectureReduction, and ModelMissReview consumption without expanding their authority.
- [x] 6.4 Refresh only affected SkillGuard source contracts/check manifests after prompt and code identities stabilize; do not edit installed consumer copies directly.
- [x] 6.5 Update API/CLI documentation, blueprint model documentation, README, release checklist, changelog, project/version metadata, and consumer-suite inventory for patch release `0.68.5`.

## 7. Focused and Affected Verification

- [x] 7.1 Run focused unit/API/CLI tests for inventory discovery, dispositions, bidirectional binding, source-independent semantics, resource closure, qualification status, reconstruction receipts, deterministic projection, and ordinary lightweight behavior.
- [x] 7.2 Run affected ExistingModelPreflight, CodeStructureRecommendation, StructureMesh, ModelTestAlignment, ModelMesh, Portable, BCL, field/UI/test handoff, self-understanding, risk, closure, and authoritative-model-system tests and models.
- [x] 7.3 Run strict OpenSpec validation and implementation verification for the blueprint, self-understanding, and contraction changes; fix every critical/warning gap rather than widening scope.
- [x] 7.4 Refresh only invalidated model/SkillGuard evidence owners and confirm later peer writes were preserved and correctly invalidated affected evidence.

## 8. Freeze, Install, Archive, and Publish

### 8.1 Model-backed release contraction

- [x] 8.1.1 Run current ExistingModelPreflight, ArchitectureReduction, CodeStructureRecommendation, and StructureMesh over the completed FlowGuard self blueprint before freezing the release snapshot.
- [x] 8.1.2 Inventory every in-scope repeated route, branch, adapter, wrapper, facade, helper, and validation path with a typed target disposition and current observable-contract evidence.
- [x] 8.1.3 Apply only equivalence- or delegating-facade-proven contractions, preserve all peer work, and keep uncertain/property-only candidates as visible maintenance obligations.
- [x] 8.1.4 Re-run every model, parity check, and focused test invalidated by the safe contractions; prove public entrypoints, outputs, state, side effects, and validation boundaries remain unchanged.

### 8.2 OpenSpec closure handoff

- [x] 8.2.1 Re-run strict validation against the current observed model authority and tracked self blueprint, with no unresolved implementation or specification warning.
- [x] 8.2.2 Sync the verified delta specs, confirm all three implementation changes are archive-ready, and dispose the empty superseded DNA change without adding a route or compatibility artifact.

Package/skill installation, the unique frozen nine-owner release gate, Git/GitHub
publication, and the predictive-KB postflight remain separate
DevelopmentProcessFlow release obligations. They run against the archived tree
and do not block OpenSpec from archiving a completed implementation change.
