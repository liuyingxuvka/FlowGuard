## 1. Current intent source authority

- [x] 1.1 Add a strict intent-source validator that rechecks safe direct project files and exact current declared WorkContext artifacts without conflating their identity schemes.
- [x] 1.2 Make model revision construction validate and freeze every contribution source before candidate output, then recheck the frozen identities before publication.
- [x] 1.3 Add positive and same-class regressions for stale, missing, absolute, escaping, linked, non-regular, WorkContext-stale/foreign, and during-build changed sources, including proof that failures write no candidate authority.
- [x] 1.4 Advance the direct-current model-regression manifest, bind every active project-file intent source to its exact logical model owner input, include those paths in focused validation and model identity, and block missing, extra, duplicate, unsafe, unresolved, or foreign-owner bindings without treating WorkContext artifacts as repository files.

## 2. Author-source shadow synchronization

- [x] 2.1 Compile the complete current author skill inventory separately from the clean consumer projection and record explicit projection-role ownership.
- [x] 2.2 Add one full-suite `author-sync` action with dry-run, exact consumer-to-author transition, already-author idempotence, transactional staging, and post-activation verification.
- [x] 2.3 Fail closed on modified owned paths, unowned collisions, unsafe links, missing authority, or ambiguous role while preserving co-located skills and every surrounding repository path.
- [x] 2.4 Add regressions for exact 15-member role conversion, consumer-only artifact removal, author-only artifact inclusion, peer-path preservation, dry-run, idempotence, conflict preservation, and rollback.

## 3. Cumulative current intent and exact denominators

- [x] 3.1 Add the strict cumulative current-intent view, typed active lineage references, explicit retain/supersede/retire transitions, compact model-owner bindings, and complete independent model-owner denominator.
- [x] 3.2 Make revision construction fold the prior complete view with the current delta, verify every active source before and after construction, and publish the view only inside the content-addressed revision pair.
- [x] 3.3 Add the one-way evidence-bound migration from the exact accepted current ancestry and make normal current-authority loading reject legacy or incomplete effective views without fallback.
- [x] 3.4 Project only the cumulative view into the self blueprint, require every current behavior block to consume intent through its exact model owner, advance the enclosing strict project-document schema with intent-inventory v5, and add current round-trip plus former-parent/child rejection coverage.
- [x] 3.5 Add native model failures and focused regressions for latest-delta-as-current, silent id replacement, missing prior transition, stale active source, missing/extra/duplicate owner binding, root fallback, empty behavior intent, and cross-owner behavior intent.
- [x] 3.6 Add one exact current-authority state loader that validates head, snapshot, accepted revision, one typed activation-or-rollback transition receipt, predecessor/head/generation bindings, current view, and optionally current source identities; make audit, plan/build, activation, and rollback consume it.
- [x] 3.7 Reverify every active current-intent source during authority audit and report source stale/missing/invalid separately from immutable revision corruption.
- [x] 3.8 Add final manifest re-read and authority-section CAS for activation and rollback, preserving unrelated peer sections while blocking a changed authority base and leaving prewritten immutable artifacts non-current.
- [x] 3.9 Make explicit legacy bootstrap traverse typed activation and rollback edges through exact predecessor-head fingerprints, ignore non-matching orphans, strictly parse every admitted historical schema, and report a damaged v4 chain as invalid rather than merely bootstrap-required.
- [x] 3.10 Reject bootstrap supersede/conflict edges outside the exact audited legacy/current graph, including ghost ids, disposition disagreement, unresolved active conflicts, and replacement-set mismatch.
- [x] 3.11 Remove derived `complete`, `evidence_complete`, and `intent_acceptance_ready` fields from authority identity wire, require strict raw JSON primitive types for all remaining fields including rollback booleans/text/ids, and add malformed-wire regressions.

## 4. Model and release closure

- [x] 4.1 Add native FlowGuard model failures and purpose bindings for stale intent source acceptance and unsafe author projection synchronization.
- [x] 4.2 Run focused intent, revision, distribution, CLI, parity, and release-composition tests and repair all failures.
- [x] 4.3 Synchronize all delta specifications to their main specifications, create or renew the exact current intent contribution, and strictly validate this change.
- [x] 4.4 Renew one exact pre-archive model authority and confirm this completed change is archive-ready after current intent-source validation, cumulative-intent coverage, author-projection contract checks, and strict OpenSpec validation; record archive path movement and real external projection as fresh release-workflow inputs.

## Post-Archive Release Handoff (not OpenSpec completion tasks)

After this change and the coordinated FlowGuard changes are archived, the active DevelopmentProcessFlow release workflow—not this implementation checklist—owns the canonical-main-spec intent refresh, final model-authority renewal, official SkillGuard contract compilation, ownership-safe real shadow author synchronization, clean local consumer installation, four-domain role-aware parity, unique foreground full release verification, publication, and postflight. No pre-archive fingerprint or external projection is final evidence for those operations.
