## 1. Confirm Membership And Route Inputs

- [x] 1.1 Verify the canonical suite inventory and route-topology/ownership verification contracts pass; record their hashes as compiler inputs.
- [x] 1.2 Inventory every current `SKILL.md`, `agents/openai.yaml`, direct reference, `.skillguard` control root, generated contract, check manifest, and cloned check script for all seventeen skills.

## 2. Build The Shared Contract Toolchain

- [x] 2.1 Define `.skillguard/contract-source.json` schema for route ids, source requirements, phase bindings, workflow stages, native checks, obligations, test gaps, blockers, layouts, and non-parallel proof.
- [x] 2.2 Implement deterministic `flowguard/skill_contracts.py` and `scripts/compile_flowguard_skill_contracts.py` generation/check modes for current work contracts, check manifests, and hashes.
- [x] 2.3 Implement shared schema/layout/native-depth checks and per-skill manifest bindings; prohibit identical generic route contracts across unrelated owners.
- [x] 2.4 Add known-bad fixtures for missing control root, old schema, generic four-route contract, stale hash, manual generated edit, missing native binding, only shared field-presence checks, and parallel SkillGuard ownership.

## 3. Complete One Pilot

- [x] 3.1 Convert `flowguard-model-test-alignment` as the pilot to the ten standard sections, route-specific contract source, current generated contract, manifest, and aligned `openai.yaml`.
- [x] 3.2 Replace the misleading TestMesh non-invocation sentence with a typed handoff boundary and update old literal-string tests to assert the new semantic contract.
- [x] 3.3 Make static skill, contract, depth, source-layout, and temporary-installed-layout checks pass for the pilot before bulk migration.

## 4. Refactor Kernel And Process Skills

- [x] 4.1 Refactor `model-first-function-flow` to the ten sections, add kernel `agents/openai.yaml`, generate/check its route index, move detailed inventories to direct references, and meet the 120-line target.
- [x] 4.2 Convert `flowguard-development-process-flow`, `flowguard-plan-detailing-compiler`, and `flowguard-agent-workflow-rehearsal` with exact mode/delegation, freshness, side-effect, evidence, and claim-boundary contracts.
- [x] 4.3 Verify PlanDetailing/AgentWorkflow cannot activate generically and progress output cannot satisfy terminal pass.

## 5. Convert Behavior And Evidence Skills

- [x] 5.1 Create the missing `.skillguard` control root and target-specific contract for `flowguard-behavior-commitment-ledger`, including six ledger modes and internal PPA evidence ownership.
- [x] 5.2 Convert `flowguard-contract-exhaustion-mesh`, `flowguard-test-mesh`, and `flowguard-model-miss-review` with finite-universe, receipt, model-miss invalidation, and typed handoff obligations.
- [x] 5.3 Ensure each behavior/evidence skill has at least one owner-specific native check and explicit downstream consumer.

## 6. Convert Structure And Ownership Skills

- [x] 6.1 Convert `flowguard-model-mesh`, `flowguard-existing-model-preflight`, `flowguard-model-topology-hazard-review`, and `flowguard-field-lifecycle-mesh` with child-reattachment, light/full search, typed target, lifecycle, and liveness gates.
- [x] 6.2 Convert `flowguard-architecture-reduction`, `flowguard-code-structure-recommendation`, and `flowguard-structure-mesh` with observable-contract, recommendation-only, facade/parity, and ownership boundaries.
- [x] 6.3 Prove direct references resolve in both repository and temporary installed layouts.

## 7. Convert UI Skill And Protocols

- [x] 7.1 Convert `flowguard-ui-flow-structure` to the standard entrypoint with greenfield/source-based/mixed modes and a strict distinction between design evidence and implemented/runnable evidence.
- [x] 7.2 Split the oversized UI protocol into observed-surface, capability/interaction, journey/structure/text, human-operability, implementation-evidence, and geometry/transition references behind a compact index.
- [x] 7.3 Split oversized kernel/reference protocols by owning route without duplicating obligations or exceeding prompt budgets.

## 8. Remove Legacy Contract Paths

- [x] 8.1 Generate current `work-contract.json` and `check_manifest.json` for all seventeen skills and delete the successful legacy generic-contract path.
- [x] 8.2 Replace the eighty cloned shallow checks with shared evidence-aware checks plus per-skill manifests; preserve no second green field-presence route.
- [x] 8.3 Add `tests/test_skill_prompt_parity.py`, `tests/test_skill_contract_compiler.py`, and `tests/test_skill_installed_layout.py`, and update `tests/test_skill_docs.py` to test the new sections and semantics.

## 9. Certify And Handoff

- [x] 9.1 Run compiler check mode, prompt/layout tests, and static/contract/depth checks for all seventeen members; require 17/17 deep-pass and zero hollow/risk/legacy/stale findings.
- [x] 9.2 Run every command in `verification-contract.yaml` and strict OpenSpec validation; bind results to suite, route, prompt, contract-source, and compiler hashes.
- [x] 9.3 Hand the certified contracts and manifests to `bind-flowguard-self-governance-evidence`; do not claim full self-governance until real receipts and parent consumption are implemented there.
