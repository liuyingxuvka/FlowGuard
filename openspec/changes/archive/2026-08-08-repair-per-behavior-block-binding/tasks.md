## 1. Current Block-Local Contract

- [x] 1.1 Advance the direct-current project and behavior blueprint schemas and add exact per-behavior-block portable bindings to each model owner.
- [x] 1.2 Validate that every binding targets an owned implementation surface, every case targets one declared block, and primary summaries exactly match the primary block binding.
- [x] 1.3 Update the strict project document writer and loader to round-trip only the new current block-local structure.

## 2. Self-Blueprint Production and Readiness

- [x] 2.1 Generate one unique good case and one unique boundary case for every observed behavior surface, plus one bad case only for each explicit surface-to-failure edge, while preserving source-case lineage.
- [x] 2.2 Generate exact surface-local input, output, and state member mappings and compare their union with the independent portable model member catalog.
- [x] 2.3 Partition owner cases by behavior block during readiness compilation and retain fail-closed rejection of foreign or nonexistent blocks.
- [x] 2.4 Bind resources, coverage edges, normalized objects, and affected projections to the complete behavior-block set without copying sibling coverage.
- [x] 2.5 Bind owner-level transitions and protected failures to one explicit composite behavior surface, keep detailed children sparse, and reject missing, foreign, or implicit sibling member edges.

## 3. Regression and Model-Miss Closure

- [x] 3.1 Add a point regression for one owner with two sibling behavior blocks and different function signatures.
- [x] 3.2 Add same-class failures for foreign blocks, missing sibling case kinds, duplicate case ids, stale field mappings, and cross-block coverage.
- [x] 3.3 Run focused project, self-blueprint, readiness, and model-test alignment tests and repair all failures.
- [x] 3.4 Separate planned parameter-case identity from owner-level source-case lineage and make every behavior dimension applicable only to its exact surface.
- [x] 3.5 Classify the complete FlowGuard implementation inventory into behavior and supporting surfaces without hiding observed state, effect, dynamic, or entry behavior.
- [x] 3.6 Pre-index owner bindings, cases, intent targets, calls, coverage, reduction signals, and candidate neighborhoods so whole-target joins do not rescan their full denominators.
- [x] 3.7 Store normalized coverage payloads once, keep shards reference-only, cache repeated large fingerprints, and retain strict current-format readers without fallback.
- [x] 3.8 Replace the architecture-reduction duplicate whole-blueprint rebuild with an exact independently recomputed build-input identity comparator.
- [x] 3.9 Add 1x/2x/4x scale regressions, real-repository count invariants, and affected-read size checks that prove near-linear growth and bounded projection size.
- [x] 3.10 Bind the composed self-blueprint plus architecture-reduction command to one complete compact audit path and regression-test its full denominator, currentness, and cross-block rejection semantics before the live release audit.
- [x] 3.11 Add two-dimensional surface/failure regressions proving `2S + E` cases and `6 * (2S + E)` coverage, including unmapped, foreign, and sibling-substitution failures.

## 4. Intent, Model Authority, and Closure

- [x] 4.1 Synchronize the three delta specs to their main specifications and preserve earlier archived contributions as historical generation-40 lineage only.
- [x] 4.2 Run strict OpenSpec validation for the still-active change and create exact current intent contributions for the frozen candidate effects.
- [x] 4.3 Bind the project intent inventory and source-authority rows to the current observed model-snapshot fingerprint while retaining source-inventory revision as a separate typed build input.
- [x] 4.4 Project the complete exact set of accepted `model-realizes-purpose` owners, reject missing or foreign owners, and exclude declared-but-unrealized relations.
- [x] 4.5 Bind each affected owner semantic specification to the exact accepted intent source id and source fingerprint without copying intent bodies to child behaviors.
- [x] 4.6 Recalibrate module/class aggregates so only an independently observed composite contract creates a composite behavior block; keep structural aggregates as supporting surfaces.
- [x] 4.7 Enforce coverage-contract ownership: every direct behavior implementation owns its exact behavior-block obligation, every helper/supporting binding references that same direct owner obligation and semantic dimensions without becoming a second owner, and planned checker design remains separate from terminal execution, including `not_run` children under a passing parent or suite.
- [x] 4.8 Add positive and negative model/point regressions for mixed subject identities, omitted realized owners, missing semantic provenance, aggregate promotion, sibling failure/result copying, and coverage-owner substitution.
- [x] 4.9 Strictly validate the revised OpenSpec artifacts and create a fresh exact intent contribution; the earlier pre-clarification validation and intent fingerprints remain historical only.
- [x] 4.10 Keep ordinary test nodes and exact model-regression evidence in separate validated namespaces, then rerun affected native model checks and the governed model parent, build exact owner evidence, accept the candidate revision, and atomically activate current pre-archive observed authority.
- [x] 4.11 Run the live compact self-blueprint plus Architecture Reduction audit and complete the model-driven cleanup review against that exact candidate authority.
- [x] 4.12 Confirm this completed change is archive-ready after its exact current intent, candidate authority, live compact self-audit, and model-driven cleanup review pass; record the future archive path movement as a fresh governed-source input owned by DevelopmentProcessFlow.

## Post-Archive Release Handoff (not OpenSpec completion tasks)

After this change and its five companion changes are archived, the active DevelopmentProcessFlow release workflow—not this implementation checklist—owns the canonical-main-spec intent refresh, final governed model parent, distinct native-owner evidence, accepted and activated observed authority, compact self audit, official SkillGuard supervision, author-shadow and consumer-install synchronization, four-domain parity, unique foreground full release validation, publication, and postflight. No pre-archive fingerprint is final evidence for those operations.
