## 1. Contract closure

- [x] 1.1 Add the compact/detail/full projection requirement without changing
  the complete candidate denominator.
- [x] 1.2 Require proof across callers, consumers, observable behavior, state,
  errors, side effects, public surfaces, models, and tests/oracles.
- [x] 1.3 Keep one current path and disallow aliases, fallbacks, and unbound
  wrappers after contraction.

## 2. Evidence boundary

- [x] 2.1 Bind the requirements to the existing ArchitectureReduction model
  and current FlowGuard cleanup route.
- [x] 2.2 Leave unresolved candidates visible when their proof is incomplete.

## 3. Path-quality optimization

- [x] 3.1 Measure bounded structural cost dimensions from every current model
  provider and record the measurement evidence fingerprint; do not infer a
  deep review only from a caller-provided flag.
- [x] 3.2 Mark each model as lightweight, deep-required, or deep-closed and
  require current trigger evidence before a deep review can close.
- [x] 3.3 Keep ordinary low-cost paths on the lightweight route while making
  high-cost, release-critical, model-miss, and necessity-gap signals visible.
- [x] 3.4 Add denominator, trigger-completeness, cost, and depth summaries to
  the self path-quality projection without embedding a large report.

## 4. Prompt and route economy

- [x] 4.1 Represent prompt material edges with explicit stage, mandatory
  status, trigger, owner, and claim fields and report catalog, preselection,
  admitted-core, and triggered-expansion budgets separately.
- [x] 4.2 Split only high-cost protocol material by exact trigger; keep the
  root as a routing shell and preserve independent satellite ownership.
- [x] 4.3 Connect the two DevelopmentProcessFlow internal modes through exact
  conditions, and migrate or retire the unowned root integration reference
  without adding aliases or fallback readers.
- [x] 4.4 Remove consumer-facing historical handoff stubs and author-only VTP
  markers after their references and SkillGuard projections are updated.

## 5. Candidate and portable-blueprint evidence

- [x] 5.1 Add a bounded candidate index to compact ArchitectureReduction
  output and an exact one-candidate detail read; preserve the full candidate
  denominator and unresolved states.
- [x] 5.2 Make portable blueprint verification one deep check per trust
  boundary, reject duplicate/non-finite JSON, and write atomically with one
  canonical fingerprint computation.
- [x] 5.3 Keep portable blueprint claims target-neutral and separate static,
  portable-integrity, executed, and optional user-requested reconstruction
  evidence.

## 6. Runner ownership and bindings

- [x] 6.1 Prove the six duplicate wrapper runners are behaviorally equivalent
  to their model entry points and rebind manifests, ledgers, docs, and tests
  to the direct model runners.
- [x] 6.2 Remove only the proven wrappers, refresh model snapshot/revision/
  activation through the native authority pipeline, and preserve the full
  generation chain.
- [x] 6.3 Keep model, code, test, BCL, public-entry, and installed projection
  identities distinct and current.

## 7. Validation and release closure

- [x] 7.1 Keep the full-validation graph on explicit evidence dependencies
  only; unrelated owners must remain reusable and independently schedulable.
- [x] 7.2 Run affected focused checks once, then one foreground frozen-parent
  full validation; do not use a second parent, unattended resume, or hidden
  background validation owner. The historical v0.68.8 closure used more than
  one parent; v0.68.9 closes this exception with one foreground parent and
  result fingerprint `sha256:4316a5b7028bd64cd37974baf7b951e0c8194cde0ffa3f7e84f098e9818eee31`.
- [x] 7.3 Synchronize the local installation and consumer projection only
  after source, model, and validation identities are frozen.
- [x] 7.4 Verify version 0.68.8, commit/tag/release identity, and publish the
  final GitHub release only after all required evidence is terminal-success.
