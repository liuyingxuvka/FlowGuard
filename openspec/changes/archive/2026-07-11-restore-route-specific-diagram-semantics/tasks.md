## 1. Restore Canonical Prompt Semantics

- [x] 1.1 Restore the kernel diagram intent gate and five route-owned edge meanings while keeping every prompt inside its hot-path budget.
- [x] 1.2 Add FlowGuard-owned source regression coverage for all required semantic anchors without weakening the LogicGuard installed-surface test.

## 2. Refresh Contract And Suite Evidence

- [x] 2.1 Regenerate the six stale deterministic SkillGuard contract families with the official compiler and confirm a subsequent check writes nothing.
- [x] 2.2 Run focused skill-documentation, prompt-parity, compiler, and canonical suite-marker checks; retain the pre-existing full static SkillGuard V2 depth blocker as not passed and out of scope.
- [x] 2.3 Run the full FlowGuard test suite after concurrent mixed-root edits stabilize, preserving peer-owned files.

## 3. Synchronize Active Installation

- [x] 3.1 Refresh the global Codex FlowGuard suite with the official whole-suite installer and prove content-exact parity.
- [x] 3.2 Re-run LogicGuard's installed FlowGuard diagram-semantics regression against the refreshed global suite.

## 4. Verify And Handoff

- [x] 4.1 Run the OpenSpec verification contract and record failures, skipped checks, residual risk, and the downstream vendored-suite synchronization commands.
