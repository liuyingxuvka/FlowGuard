## 1. Single-Observation Blueprint Build

- [x] 1.1 Introduce one immutable invocation-local implementation observation bundle and let declaration classification plus inventory review consume it without a second provider parse.
- [x] 1.2 Make the self-blueprint carry the exact identity of the inputs it consumed and remove the duplicate composed pre-build capture.
- [x] 1.3 Keep one final fresh governed-input comparison and add mutation regressions that block publication on any covered change.

## 2. Single-Owner Identity and Projection

- [x] 2.1 Reuse one normalized behavior reference dictionary for fingerprint, byte count, integrity, and physical projection.
- [x] 2.2 Compute and store one full architecture-review fingerprint without reconstructing the complete identity payload twice.
- [x] 2.3 Emit distinct required full-review and compact-projection fingerprints without invoking a large fingerprint property from compact projection.
- [x] 2.4 Make the release self-maintenance owner request complete composed review with compact machine-readable publication and bind both review identities explicitly into its validation-owner receipt.

## 3. Exact Relation Contraction

- [x] 3.1 Aggregate ambiguous call evidence by raw alias while preserving exact caller and candidate sets plus the unchanged blocker.
- [x] 3.2 Index branch-to-surface containment, memoize surface owner bindings, and materialize candidate fingerprints once per invocation.
- [x] 3.3 Group retain rows only across identical authority/evidence and prove exact one-time universe-member coverage.
- [x] 3.4 Remove the unused supplied-blueprint path that rebuilt a second bundle and keep one direct standalone path plus one direct composed path.

## 4. Verification and Authority Closure

- [x] 4.1 Add operation-count and same-class regressions for observation count, currentness capture count, identity construction, ambiguity representation, and compact release composition.
- [x] 4.2 Run focused blueprint, reduction, projection, and release-composition tests and repair every failure.
- [x] 4.3 Synchronize the delta specifications, create an exact current intent contribution, and strictly validate the active change.
- [x] 4.4 After all functional and structural corrections are stable, renew the exact live candidate model authority and distinct native-owner evidence once, then execute one complete compact self-blueprint plus Architecture Reduction audit without claiming it as the final release receipt.
- [x] 4.5 Confirm this completed change is archive-ready after the validated live audit and current linear candidate-evidence closure pass; record the future archive path movement as a fresh governed-source input owned by DevelopmentProcessFlow.

## 5. Supervised Runtime Closure

- [x] 5.1 Cache large immutable inventory, binding, manifest, alignment, topology, intent, and target identities once per object without changing canonical fingerprints.
- [x] 5.2 Index model-test blocker findings once and independently validate each affected shared object once before reusing its exact computed fingerprint.
- [x] 5.3 Replace repeated contract unions, ready-candidate set construction, candidate identity reads, and conflict-wide rescans with exact one-time indexes.
- [x] 5.4 Add structural operation-count regressions and rerun the affected blueprint, alignment, reduction, topology, readiness, and target suites.
- [x] 5.5 Prove through the complete live compact self-audit that the bounded projection reaches a terminal result without reducing any denominator or check; reserve the formal producer receipt for the unique frozen post-archive release validation.

## 6. Linear Candidate Evidence Neighborhoods

- [x] 6.1 Introduce one direct-current content-addressed candidate evidence-neighborhood catalog for test ids, coverage ids, dimensions, and current receipt ids, and bump the self-architecture-reduction review schema.
- [x] 6.2 Replace per-candidate copies with exact neighborhood id/fingerprint references and use a typed composite observable-contract identity whose proof consumer resolves the complete semantic contract.
- [x] 6.3 Make review and proof consumers resolve one exact catalog row and block missing, duplicate, foreign, stale, ambiguous, inline-fallback, or fingerprint-mismatched evidence.
- [x] 6.4 Add a shared-behavior scale regression proving physical evidence edges are O(E), not O(C×E), plus mutation regressions for every reference-integrity failure.
- [x] 6.5 Separate audit completion, candidate action authorization, and cleanup release readiness; prove that proofless unresolved candidates remain visible without failing a complete audit, while safe-unapplied actions and audit-input gaps still block.
- [x] 6.6 Run the affected focused reduction, proof, and compact checks; after all owned functional source changes are frozen, renew exact live candidate authority once and close 4.4 and 5.5 from that same composed-audit result rather than repeatedly rebuilding authority during repair.

## 7. Dynamic Selectors and Test/Check Ownership Semantics

- [x] 7.1 Extend finite-selector analysis to preserve exact domains through supported comprehension bindings and make the only genuinely open self selector finite at the implementation boundary.
- [x] 7.2 Materialize exact-current dynamic-selector contracts from the Python provider observation and retire manually duplicated finite-selector contract rows from the self definition.
- [x] 7.3 Keep supporting and scoped-out test dispositions out of the check-owner gap path, report typed behavior-coverage owner gaps only where that layer requires them, and preserve every required test node independently.
- [x] 7.4 Aggregate each native execution owner once with a current design fingerprint while preserving run status and terminal receipt evidence as separate facts; remove per-case `check-design-owner` duplication.
- [x] 7.5 Add focused regressions for finite and open selectors, mechanically stale contracts, supporting tests, missing behavior owners, shared execution owners, `not_run` design identity, broad-glob non-ownership, and universe-size contraction.
- [x] 7.6 Rerun the complete live self audit and classify every remaining reduction candidate through proof-backed contract, retain, or unresolved action without restoring a duplicate audit or release-owner route.

## 8. Semantic Necessity and Candidate Quality

- [x] 8.1 Add a direct-current self-reduction necessity witness that binds current intent, exact behavior/model/code ownership, source-independent semantics, and model-code-test evidence without candidate identity; keep callers and active external commitments as exact contraction context rather than a universal representation prerequisite.
- [x] 8.2 Replace `public_contract`, structural owner-binding retain, and structural-id difference with witness-backed singleton retain and pairwise `different_current_semantics` candidate retain.
- [x] 8.3 Make caller and public-entrypoint identity part of the typed candidate binding; block contraction authorization on caller ambiguity, public-without-current-promise, and partially repeated candidate groups without misclassifying framework or protocol dispatch as absent software.
- [x] 8.4 Treat maintenance-like names as classification signals only and require an exact shared call or structure relation before materializing a candidate.
- [x] 8.5 Add regressions proving that path, owner, model, intent, spec, oracle, test, receipt, and candidate ids do not manufacture semantic differences; cover true semantic differences, supporting relations, caller gaps, public commitments, and three-member partial repeats.
- [x] 8.6 Run the focused necessity/candidate/step/compact suites and inspect the complete live candidate counts after proof-store integration.

## 9. Canonical Persistent Proof Closure

- [x] 9.1 Discover strict current proof records from the canonical aggregate validation-owner receipt store and remove caller-supplied proof-record injection.
- [x] 9.2 Freeze one bundle and candidate inventory per explicit batch, select candidates by exact id and fingerprint, reuse exact-current aggregate evidence before execution, and publish deterministic candidate proofs under one aggregate owner.
- [x] 9.3 Ignore stale receipts as history, block duplicate exact-current authorities, and add restart, reuse, subset, mismatch, and duplicate regressions without creating a second registry.
- [x] 9.4 Run the complete focused proof and self-architecture suites, then use the read-only composed audit to confirm that persisted proof authority is automatically visible.

## 10. Actionable Compact Gaps and Supporting-Oracle Boundary

- [x] 10.1 Keep supporting test/oracle implementation surfaces in the DNA while preventing their inherited oracle from being misread as self-certification of an independent behavior block.
- [x] 10.2 Add bounded static-blocker counts, separately bounded execution-gap counts, and one bounded example per emitted child finding kind to the compact self-qualification projection; keep planned `not_run` execution visible without misreporting it as a static DNA defect, and add focused regressions that forbid complete report expansion.
- [x] 10.3 Record one deterministic typed first-failure necessity gap per unresolved implementation surface and publish exact per-kind counts with at most eight representative member ids, without expanding or rebuilding the full review.
- [x] 10.4 Scope planned coverage joins by the exact behavior block and implementation surface, prevent sibling test inheritance, preserve model-only evidence separately, and add positive plus negative focused regressions.
- [x] 10.5 Unify blueprint and reduction caller resolution, prefer exact same-file and import-bound identities over repository-wide short names, admit bounded first-class and annotated-receiver references, and keep caller parity as contraction proof rather than a universal current-necessity gate.
- [x] 10.6 Renew affected model authority and confirm through one composed audit that the systematic self-oracle blocker is gone and any remaining contraction work is independently actionable.

## Post-Archive Release Handoff (not OpenSpec completion tasks)

After this change and its five companion changes are archived, the active DevelopmentProcessFlow release workflow—not this implementation checklist—owns the canonical-main-spec intent refresh, final observed model-authority renewal, official SkillGuard supervision, ownership-safe author-shadow synchronization, clean consumer installation, source/formal/shadow/installed role-aware parity, unique foreground full release verification, publication, and postflight. Archive path movement is a fresh governed-source input, so no pre-archive fingerprint, authority receipt, or external projection is final evidence for those operations.
