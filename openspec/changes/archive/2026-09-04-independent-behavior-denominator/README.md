# independent-behavior-denominator

Add one native independent behavior denominator to the current Behavior
Commitment Ledger authority so broad behavior claims cannot be derived only
from existing ledger rows or tests.
