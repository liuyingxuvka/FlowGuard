## 1. Freeze Baseline and Red-Test Contract

- [x] 1.1 Record the authoritative repository, source commit, installed engine import path/schema, active OpenSpec change, `unit:flowguard-suite`, existing observed-model head, consumer/install identities, current version, and peer worktree boundaries without cleaning or resetting peer changes.
- [x] 1.2 Refresh the task-owned affected-component and validation-owner plan after implementation discovery so every final production file, model, skill, test, projection, install, and release check maps to one exact owner; make unmapped or duplicate ownership block before execution.
- [x] 1.3 Add aggregate false-green tests proving a blocked required native report makes project and self whole qualification incomplete while retaining the exact lower-layer results and first gap.
- [x] 1.4 Add provider-authority red tests for strict artifact loading, fingerprint tampering, payload drift, provider-version drift, registry drift, and provider result self-certification.
- [x] 1.5 Add target-profile red tests proving a non-code workflow needs only its real workflow layers, rejects a software-profile plan, and blocks when one required workflow layer is absent.
- [x] 1.6 Add implementation-inventory red tests for a missing adapter, adapter exception, adapter identity mismatch, implementation-bearing file with disabled discovery and zero surfaces, and a successful synthetic non-Python/non-code adapter.
- [x] 1.7 Add topology red tests for unknown ports, schema mismatch, stale child/runtime evidence, missing reattachment, multiple structural parents, structural cycles, and feedback without a progress contract.
- [x] 1.8 Add behavior/evidence red tests for same-shape semantic errors, swapped owners/outcomes, missing leaf assertions, parent receipt substitution, receipt relabeling, zero collection, skip/xfail/not-run, and static-ready/execution-not-run separation.
- [x] 1.9 Add compact/reduction red tests proving affected reads do not call whole builders or unrelated loaders and cleanup completeness is not derived from its own candidate list.

## 2. Target-Neutral Provider and Profile Core

- [x] 2.1 Add current-schema `TargetSystemLayerPlan` validation, canonical fingerprinting, strict load/serialization, profile matching, ordered layer validation, and claim-boundary identity in `flowguard/target_system_blueprint.py`.
- [x] 2.2 Define bounded canonical software and non-code-workflow reference plans without embedding language-specific fields or target-product role catalogs in the kernel.
- [x] 2.3 Add strict current-schema loaders for `TargetSystemProviderResult`, `TargetSystemProviderRegistry`, and `TargetSystemSnapshot` that reject unknown/missing keys and supplied-fingerprint drift.
- [x] 2.4 Add a frozen provider-artifact aggregate that stores pre-established artifacts but never invokes an adapter, creates a provider result, or rebuilds registry/snapshot authority.
- [x] 2.5 Keep target-system assembly private, make the public native qualifier consume the exact frozen plan, registry, results, and snapshot, remove dependence on the global software-only order, and preserve exact stale/missing/profile gaps.
- [x] 2.6 Extend generic implementation inventory dispositions so every implementation-bearing or supporting member has current adapter discovery, a typed bounded not-applicable reason, or a terminal non-implementation disposition.
- [x] 2.7 Reject `requires_adapter=False` as an empty-success escape for implementation-bearing members and keep missing/failed/mismatched adapters in the independent denominator.
- [x] 2.8 Extend the generic inventory contract to accept a synthetic non-Python software surface and a non-code workflow surface without Python module, symbol, path, or pytest semantics.
- [x] 2.9 Run focused target-system and implementation-inventory tests and repair every red contract without adding compatibility readers, aliases, or fallback adapters.

## 3. Hierarchy, Interfaces, and Graph Closure

- [x] 3.1 Extend blueprint topology nodes with exact typed input/output catalogs, schema or portable-refinement identities, and state/effect ownership while reusing existing hierarchy evidence types.
- [x] 3.2 Extend topology relations with exact producer-output to consumer-input mappings, transformation/refinement identity, child evidence, runtime-path evidence, and optional loop/progress contracts.
- [x] 3.3 Validate that every mapped producer output and consumer input exists and that schemas match or have a current exact transformation/refinement contract.
- [x] 3.4 Require every child validation/runtime id to exist in the current evidence registry, match the exact child/owner fingerprint binding, close child-output reattachment, and preserve one structural parent for each structural child.
- [x] 3.5 Reject self, two-node, and longer structural cycles; accept data/control feedback only with explicit finite-bound, progress, repair-token, terminal blocker, or fairness evidence.
- [x] 3.6 Project topology gaps into readiness so a locally green child with stale or incomplete parent/sibling reattachment cannot qualify the whole target.
- [x] 3.7 Compile directional affected edges for parent-child, producer-consumer, delegation, and support relations; include the causing relation object; prove ancestor/sibling propagation plus cycle and duplicate-edge stability in the focused topology/hierarchy regression set.

## 4. Independent Semantics, Intent, Resources, and Completeness

- [x] 4.1 Build the observed target denominator from boundary/provider discovery independently of behavior declarations, covering behavior-bearing surfaces, workflow steps, transitions, tests, resources, providers, and public/supporting entries.
- [x] 4.2 Compare observed members with declared models and bindings in both directions and report exact observed-but-undeclared and declared-but-unobserved members without allowing declared-set shrinkage to improve completeness.
- [x] 4.3 Bind every admitted behavior to `Input x State -> Set(Output x State)`, effects, errors, progress/termination obligations, a primary implementation or external owner, and explicitly owned supporting surfaces.
- [x] 4.4 Replace name/text-marker independence shortcuts with identity comparisons among `SemanticSpecReference`, `OracleReference`, intent source, implementation source, owner, and content fingerprints.
- [x] 4.5 Generate or require falsifiable boundary/bad cases that catch operator changes, swapped same-shape outcomes, wrong owner, wrong state transition, wrong error, and wrong side effect.
- [x] 4.6 Bind resource inventory rows to exact consuming models/behaviors and make missing, stale, or unavailable required files, configs, services, data, schemas, executables, and runtime assets block only the affected closure.
- [x] 4.7 Bind intent contributions from explicit user objectives, OpenSpec, Spark/OpenSpark-style sources, changelog, and existing target contracts to exact affected models; expose stale or contradictory intent rather than selecting a fallback authority.
- [x] 4.8 Add independent external software, synthetic non-Python, and non-code workflow fixtures that exercise the public contracts without importing FlowGuard self-only helpers.
- [x] 4.9 Run focused semantic/resource/intent/completeness fixtures, including same-shape wrong-behavior mutations, and repair all false-green paths.

## 5. Exact Model-Code-Test Evidence and Revision Ownership

- [x] 5.1 Add separate behavior-blueprint `pre_code_status` and `executed_evidence_status` fields whose meanings match Model-Test Alignment: complete design may be ready while current execution remains `not_run`.
- [x] 5.2 Make every coverage row bind one behavior, semantic rule, external contract, implementation surface, concrete case, oracle, dimension checker, exact test/native member, and execution owner.
- [x] 5.3 Publish each passing coverage leaf only from its in-process bounded supervisor result; bind exact contract command/cwd, terminal result, successful containment query, empty descendant ids, currentness, eligibility, owner, subject, and covered member, while keeping non-pass recording unable to accept `pass`.
- [x] 5.4 Reject validation-parent receipts as leaf behavior evidence, reject a receipt reused across different owners, and reject a receipt whose covered-member set omits the current coverage row.
- [x] 5.5 Require ordinary behavior test nodes to have one behavior owner; allow multi-owner sharing only through explicit `cross_owner_integration` rows whose owner and coverage sets are exact and complete.
- [x] 5.6 Keep `ProjectTestInventory` as design/discovery inventory and keep execution receipts in the external evidence layer; do not create a second receipt system.
- [x] 5.7 Add a ModelRevisionSet invariant that one exact leaf receipt belongs to one native owner path; permit one owner receipt to cover several of that owner's affected ids only as one merged reference.
- [x] 5.8 Change `model_revision_builder.py` so the parent regression receipt proves only the parent execution and is never copied into every native-owner `RevisionEvidenceRef`.
- [x] 5.9 Require an exact current leaf receipt for every affected native model owner before a revision is evidence-complete or accepted; keep missing-owner rows visibly proposed/incomplete.
- [x] 5.10 Recompose parent TestMesh and model-revision evidence from exact leaf receipts and one canonical full-parent execution receipt, preserving skipped/not-run/blocked rows and preventing scoped-parent relabeling or aggregate command success from manufacturing members.
- [x] 5.11 Run focused behavior, Model-Test Alignment, TestMesh, model authority, and revision-builder tests, including cross-owner and receipt-fingerprint adversarial cases.

## 6. Truthful Readiness, Affected Reading, and Compact AI Entry

- [x] 6.1 Create one typed ordered readiness ledger whose rows reference exact native report identities, design status, executed-evidence status, gap ids, and downstream admission boundaries.
- [x] 6.2 Derive project/self `ok`, deepest proven layer, first gap, gap count, and full gap set mechanically from the exact ordered prefix plus every required cross-cutting native report.
- [x] 6.3 Add integrity findings when a wrapper omits or substitutes a supplied required report and prevent later passing rows from masking an earlier incomplete or stale layer.
- [x] 6.4 Create `AffectedBlueprintReader` over the normalized content-addressed index with exact affected ids, shard/object loaders, fingerprint verification, ancestor closure, and no whole-builder fallback.
- [x] 6.5 Prove with loader spies that one-owner reads access only the required shard/object ids and fail visibly on missing objects or hash drift.
- [x] 6.6 Create bounded compact projections for understanding, project/self qualification summaries, and reduction review directly from the normalized index/ledger without full `to_dict()` conversion.
- [x] 6.7 Update the AI-facing understanding result to show target, affected members, ordered layer statuses, deepest proven depth, first unresolved gap, complete gap count, and whether implementation is admitted for that exact scope.
- [x] 6.8 Keep whole blueprint materialization tied to explicit blueprint creation/export/qualification or named self/release obligations while leaving ordinary affected work on the direct reader.
- [x] 6.9 Run focused readiness, affected-loader, compact-projection, prompt-budget, and memory/materialization tests.

## 7. Public Project Path, Self Consumer, and Acceptance

- [x] 7.1 Extend project blueprint evidence and loading to accept externally frozen provider results, registry, layer plan, and target snapshot; independently rederive the current project-native provider results during qualification and block any internally consistent but current-project-divergent payload, input, capability, registry, kind, or version.
- [x] 7.2 Require the project builder to consume implementation inventory review, behavior, binding, topology, resource, intent, model-test, and readiness ledger results without silently omitting a blocker.
- [x] 7.3 Extend public CLI/API inputs to select or supply a real adapter/provider artifact and fail with the exact missing capability instead of hardcoding Python or interpreting unsupported targets as empty.
- [x] 7.4 Route software projects through the canonical software layer plan and non-code workflows through the canonical workflow plan; reject fabricated substitute layers.
- [x] 7.5 Refactor the FlowGuard self provider to gather and freeze its target-specific artifacts before passing them to the same public project/target compiler.
- [x] 7.6 Remove self-only success logic and ensure all self convenience status fields derive from the canonical readiness ledger and preserve Model-Test Alignment/topology/resource/intent gaps.
- [x] 7.7 Compose explicit self qualification, compact summary, and cleanup from one immutable self blueprint instance and block if any consumer sees a different source/provider/inventory identity.
- [x] 7.8 Make the external Python software, synthetic non-Python software, and genuine non-code workflow fixtures pass through the public entry while each missing behavior/provider/resource/test negative fixture fails independently.
- [x] 7.9 Run focused project, CLI, self-blueprint, external-target, non-Python, and non-code acceptance tests and capture exact residual gaps.
- [x] 7.10 Retire raw-manifest blueprint check/export authority and expose one explicit deterministic project export that consumes the canonically assembled project bundle, preserves every layer and readiness gap, and reports materialization separately from model depth.
- [x] 7.11 Retire the empirical reconstruction status/receipt/readiness product branch from the blueprint API, models, prompts, commands, and lifecycle specs; express sufficiency only through canonical layers, deepest proven depth, and exact gaps with no compatibility reader.
- [x] 7.12 Add the provider-neutral `target-system-blueprint-export` entry over the existing `CanonicalBlueprintProjection` envelope/writer/verifier, preserving strict descriptor, frozen evidence and plan, complete native report set, and compiler-owned readiness for deterministic non-Python software and non-code workflow exchange without caller-authored status or a second authority.
- [x] 7.13 Replace the public static-manifest qualification constructor with one private-token-derived child report, remove generic success wording, bind an exact negative claim boundary, update the self model, and prove the retired constructor is absent from the public API.

## 8. Independent Self Cleanup and Architecture Contraction

- [x] 8.1 Add `SelfReductionUniverse` built independently from public entries, exports, providers, observed implementation/supporting surfaces, models/check owners, resources, and tests, with its own canonical fingerprint.
- [x] 8.2 Keep candidate discovery independent and require the candidate inventory fingerprint to differ from the universe fingerprint; preserve parse/dynamic/adapter/owner gaps as unresolved universe members.
- [x] 8.3 Require one `retain`, `contract`, or `unresolved` disposition for every universe surface and compute cleanup completeness from universe coverage rather than candidate-id self-comparison.
- [x] 8.4 Attach candidate-unique observable contract, callers/consumers, input/output/state/effect/error coverage, exact candidate test nodes, and current equivalence or public-facade delegation proof to every proposed contraction; execute the exact test and combined parity children under supervision, compose them in the one canonical proof store, reject alternate roots/unrelated tests/missing children, and recheck all governed inputs immediately before publication.
- [x] 8.5 Route public-entrypoint or large structural candidates through StructureMesh and lifecycle/release-sensitive candidates through DevelopmentProcessFlow before applying any contraction.
- [x] 8.6 Implement and test a self-blueprint-driven audit of duplicate paths, fallback/alias/compatibility routes, repeated builders, redundant branches, dead helpers, and unnecessary serialization, while proving that similarity or size alone never authorizes deletion.
- [x] 8.7 Implement and test proof-ready contraction admission that requires peer-safe ownership, exact observable-contract and parity proof, affected model/structure/lifecycle/topology/test routing, and explicit retain/unresolved disposition for drifted or unsupported candidates; leave application to the stable-candidate release handoff.
- [x] 8.8 Implement and test the bounded compact cleanup/release-review contract so unexplained universe members and safe-unapplied contractions remain visible blockers under one self-blueprint identity; leave the actual stable-candidate review to the DevelopmentProcessFlow handoff.

## 9. Prompts, Models, SkillGuard, and OpenSpec Product Closure

- [x] 9.1 Update `flowguard` source prompt/protocols to teach one canonical living target blueprint, true affected loading, explicit whole qualification, target-owned roles, provider gaps, and depth/readiness reporting through the same primary path.
- [x] 9.2 Update `flowguard-existing-model-preflight` to consume the normalized index and exact affected neighborhood directly and to surface insufficient depth/provider evidence before admission.
- [x] 9.3 Update `flowguard-model-test-alignment` to require exact behavior/checker/test/owner/receipt binding and static-design versus execution separation.
- [x] 9.4 Update `flowguard-architecture-reduction` to require an independent universe and `retain/contract/unresolved` disposition with proof-ready handoff.
- [x] 9.5 Update `flowguard-structure-mesh` to accept only proof-ready reduction candidates with observable-contract and consumer parity evidence.
- [x] 9.6 Update `flowguard-development-process-flow` to use ordinary affected-only validation, one explicit whole self/release qualification, peer-write invalidation, and one frozen final full gate.
- [x] 9.7 Update only affected `contract-source.json` mappings for new prompt/protocol/implementation/test components; use the official SkillGuard author workflow to generate contracts/manifests and never hand-edit generated authority files.
- [x] 9.8 Run the affected source-skill native semantic checks, prompt budgets, route parity/topology, direct-current/no-fallback checks, and the six-member static SkillGuard contract audit; preserve the explicit boundary that this pre-archive product check does not substitute for post-archive native-owner receipts, full model-parent authority, installation, or final release evidence.
- [x] 9.9 Complete the `$openspec-verify-change` completeness/correctness/coherence comparison, reconcile every product task against current code and focused evidence, and run `openspec validate strengthen-target-system-blueprint-integrity --strict --json`.
- [x] 9.10 Confirm all sixteen delta specifications are ready for archive-time synchronization, with every remaining main-spec drift named explicitly; leave archive, model-authority activation, installation, final validation, and publication to the DevelopmentProcessFlow handoff below.

## DevelopmentProcessFlow Release Handoff (not OpenSpec completion tasks)

These are ordered post-product-lifecycle operations. They are not OpenSpec
implementation checkboxes and SHALL NOT be marked complete merely to permit
archive.

1. Archive `strengthen-target-system-blueprint-integrity` with main-spec synchronization and strict validation; preserve the immutable archived path and re-read every source fingerprint changed by archive.
2. Compile and check the clean consumer-suite authority, refresh any exact skill contract that consumes it, close the affected SkillGuard author unit with exact native-owner evidence, and refresh/check the private global router without projecting author evidence into consumers.
3. Refresh the archived model-intent inventory against the synchronized `target-system-blueprint` main spec, build, accept, and activate one atomic observed `ModelRevisionSet` from the exact full model parent and distinct native-owner leaf receipts, and verify one current observed head.
4. Dry-run and synchronize shadow, formal, and installed consumer skills plus the editable Python package; verify source, consumer projection, installed skill, installed package, version, schema, and CLI identities separately.
5. Re-run the composed self-blueprint and independent self-reduction review from the stable observed candidate; resolve every unexplained universe member, safe-unapplied contraction, duplicate, fallback, obsolete branch, or readiness gap before freeze.
6. Freeze version, changelog, docs, source, toolchain, OpenSpec archive, observed model head, SkillGuard plan and receipts, consumer authority, installations, impact plan, and one final validation request; run exactly one foreground full gate.
7. Verify the immutable local release candidate, stage only task-owned paths, commit `main`, create and push the patch tag, publish the GitHub Release, verify every remote, local, and installation identity separately, perform the single KB postflight, and close the goal only from current published evidence.
