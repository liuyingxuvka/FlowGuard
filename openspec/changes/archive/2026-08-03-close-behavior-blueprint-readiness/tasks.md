## 1. Freeze Authority And Recovery Boundary

- [x] 1.1 Preserve the initial staged-path inventory and peer-write boundary; record observed model, package, install, Git, tag, and release identities separately.
- [x] 1.2 Add exact external-interruption incident and settlement schemas with zero-descendant, plan, lease, owner, process, and operator-reason bindings.
- [x] 1.3 Add an all-or-nothing settlement API/CLI that accepts only named dead-process residual leases and never creates passing evidence.
- [x] 1.4 Separate child and parent current-pointer authority and prevent incomplete child success from being verified as parent completion.
- [x] 1.5 Add interruption, residual-lease, foreign-plan, live-process, partial-result, and child-pointer misuse regressions.
- [x] 1.6 Use the new path to settle only the ten audited v0.68.6 residual leases and verify the interrupted incident is non-reusable.

## 2. Behavior-Level Blueprint Contracts

- [x] 2.1 Add behavior-block contract, dimension-applicability, supporting-helper relation, and owner-structure/behavior-closure result schemas.
- [x] 2.2 Derive exact behavior-bearing versus supporting surfaces without creating external contracts for pure helpers.
- [x] 2.3 Replace generic owner-wide semantics as completion evidence with surface-specific `Input x State -> Set(Output x State)` contracts and typed not-applicable reasons.
- [x] 2.4 Add regressions showing generic semantics, missing dimensions, orphan helpers, hidden writers/effects, and duplicate owners cannot close behavior-block qualification.

## 3. Exact Model-Code-Test Evidence

- [x] 3.1 Add normalized shared semantic, oracle, test-node, assertion/native-member, case, receipt, and behavior-coverage objects.
- [x] 3.2 Stop copying an owner's entire test evidence set to every implementation surface; emit explicit per-surface coverage edges instead.
- [x] 3.3 Distinguish admitted test source, checker/assertion definition, and current terminal execution disposition without treating `not_run` as pass.
- [x] 3.4 Add exact terminal dispositions for every required project test node, including cross-owner integration, supporting, duplicate, scoped-out, and blocked rows.
- [x] 3.5 Extend Python test discovery for unittest error/warning assertions, pytest raises, subtests, fixtures, and parameter/case identities where statically recoverable.
- [x] 3.6 Add false-coverage, shared-test, unbound-test, missing-leaf-receipt, native-runner-only, and helper-support regressions.

## 4. Intent Resource Candidate And Readiness Closure

- [x] 4.1 Add an independent project resource inventory with category/member fingerprints and current/external/scoped-out/blocked dispositions.
- [x] 4.2 Bind current intent inventory and dispositions into project blueprints and ModelRevisionSet acceptance; require an evidence-bound no-intent rationale for non-trivial empty revisions.
- [x] 4.3 Add read-only candidate blueprint generation for supported Python projects with unresolved-by-default inferred owners and semantics.
- [x] 4.4 Add pure reconstruction-readiness review returning ready/incomplete/stale/blocked, every gap, deepest proven layer, first gap, and no reconstruction side effects.
- [x] 4.5 Detect same-source semantic/oracle circularity and omitted resource categories.
- [x] 4.6 Add CLI and documentation for candidate discovery and readiness review while keeping export and empirical reconstruction separately explicit.

## 5. Projection Efficiency And External Project Proof

- [x] 5.1 Normalize shared owner/semantic/oracle/test/receipt objects and shard behavior bindings by content identity.
- [x] 5.2 Preserve canonical logical fingerprint equivalence across normalized physical layouts and affected-only loading.
- [x] 5.3 Add size/duplication telemetry and regressions proving the full binding projection no longer repeats owner-wide evidence per surface.
- [x] 5.4 Extend the non-FlowGuard Python fixture through candidate generation, behavior closure, readiness gaps, and exact model-code-test review.

## 6. FlowGuard Self-Model Skills And Reduction Evidence

- [x] 6.1 Update the FlowGuard self-blueprint definition and self model to consume behavior-block, exact test, intent, resource, readiness, and empirical-not-run identities through the project-neutral path.
- [x] 6.2 Generate a machine-readable ArchitectureReduction self-review bound to the exact self-blueprint fingerprint and independently complete candidate denominator.
- [x] 6.3 Add `self_blueprint` and `architecture_reduction_review` as explicit final validation children without adding reconstruction.
- [x] 6.4 Update affected FlowGuard kernel/satellite prompts, references, native checks, SkillGuard contracts, and clean consumer projections with direct-current semantics.
- [x] 6.5 Run affected FlowGuard models, focused tests, route/native skill checks, SkillGuard affected-only validation, and architecture-reduction review; repair every failure.

## 7. OpenSpec Model Authority And Documentation Closure

- [x] 7.1 Update user and developer documentation, README/version references, release checklist, and adoption records with the four distinct depth claims and compact loading route.
- [x] 7.2 Validate the OpenSpec change strictly, verify implementation against proposal/design/specs/tasks, and sync delta specs to the main specifications.
- [x] 7.3 Build and validate one atomic ModelRevisionSet with non-empty current intent lineage, complete affected topology, current owner evidence, and observed-head activation last.
- [x] 7.4 Rerun the self-blueprint/readiness/reduction checks against the activated observed snapshot and freeze their exact fingerprints.

## 8. Installation Distribution And Frozen Validation

- [x] 8.1 Synchronize package/version metadata to the next patch version and update changelog/release notes without altering unrelated peer work.
- [x] 8.2 Synchronize the editable local package, clean installed FlowGuard consumer skills, and isolated shadow workspace transactionally.
- [x] 8.3 Verify source/package/manifest/install/consumer/shadow identities independently and run README/privacy/public-boundary checks.
- [x] 8.4 Prepare the post-archive source, toolchain, environment, impact-plan, validation-input, release-tree, and exact owner-plan freeze contract; no pre-archive fingerprint is treated as final evidence.
- [x] 8.5 Confirm that the frozen foreground full-parent contract contains the existing nine owners plus self-blueprint and architecture-reduction-review, cannot launch reconstruction, and requires a newly frozen identity after any repair.

## 9. Post-Archive Execution Boundary

After every specification and source task above is complete, archive this
change before freezing the release source identity. The active execution goal,
not this archived implementation checklist, owns the one foreground full
parent run, intentional Git commit, annotated patch tag, GitHub source-only
release, remote reconciliation, and predictive-KB postflight. None of those
external actions can retroactively turn a skipped or failed check above into a
pass.
