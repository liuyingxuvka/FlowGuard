## 1. Specification and authority grounding

- [x] 1.1 Validate the OpenSpec proposal, design, six delta specs, and task graph.
- [x] 1.2 Run full Existing Model Preflight and bind the active BCL commitments, observed model instances, affected siblings, and reuse decisions.
- [x] 1.3 Freeze the token-efficiency failure boundary and update the existing FlowGuard owner models plus candidate ModelRevisionSet.
- [x] 1.4 Freeze the DevelopmentProcessFlow/TestMesh validation owners, synchronization domains, and final release claim boundary.

## 2. Bounded model recall

- [x] 2.1 Change Existing Model Preflight to perform plane-first owner selection before detailed model materialization.
- [x] 2.2 Add distinct light/full projections and an explicit broad-inventory mode limited to bootstrap, gap backfill, or authority audit.
- [x] 2.3 Add known-good and known-bad tests for bounded default recall, changed-path closure, ambiguity, and explicit broad inventory.

## 3. Compact validation evidence

- [x] 3.1 Add a canonical compact terminal result-envelope projection with status, counts, identities, artifact path/hash, and non-pass child ids.
- [x] 3.2 Stop embedding successful child stdout and duplicate parsed payloads in suite/model terminal output while retaining complete result artifacts.
- [x] 3.3 Keep success receipt diagnostics empty after complete streams are stored and preserve bounded diagnostics for every non-pass class.
- [x] 3.4 Add output-size and failure-visibility tests for suite checks, model regressions, and evidence lifecycle receipts.

## 4. Prompt and context efficiency

- [x] 4.1 Add a representative route-bundle manifest and deterministic byte, character, line, and conservative token-estimate telemetry.
- [x] 4.2 Replace isolated-only prompt gates with combined first-read bundle budgets while retaining useful component diagnostics.
- [x] 4.3 Extract repeated cross-route gates into guaranteed-loaded shared guidance and keep every satellite's route-specific owner/trigger/claim boundary.
- [x] 4.4 Require explicit WorkContext behavior-source admission while preserving unmapped artifacts as fingerprinted context/freshness inputs.
- [x] 4.5 Make BCL discovery breadth executable from ledger mode and prevent existing-ledger omission from silently selecting bootstrap.

## 5. Reuse and model-topology closure

- [x] 5.1 Verify test-result reuse against immutable producer receipts and independently recomputed current fingerprints.
- [x] 5.2 Keep historical overlapping optimization models as historical/regression evidence and remove them from ordinary active-owner recall.
- [x] 5.3 Update affected model, mesh, ledger, field/test/process relations and activate the complete candidate ModelRevisionSet only after current evidence passes.

## 6. Maintained-skill and focused validation

- [x] 6.1 Compile the SkillGuard author contract and freeze an affected-only maintenance-unit validation plan.
- [x] 6.2 Run OpenSpec validation, affected FlowGuard models, focused unit tests, prompt budgets, output-size checks, and consumer-projection audits.
- [x] 6.3 Fix all focused failures and complete an OpenSpec completeness/correctness/coherence verification with no critical issues.
- [x] 6.4 Add and execute the UI aggregate shard-safety proof, then enable parallel execution only after serial/parallel equivalence, artifact isolation, and zero shared mutation pass.

## 7. Frozen release verification

- [x] 7.1 Freeze source, toolchain, check inventory, dependencies, and exactly one owner for each heavy check.
- [x] 7.2 Run exactly one final full FlowGuard model validation owner and one final full pytest owner; preserve immutable terminal receipts.
- [x] 7.3 Run the single frozen SkillGuard full-unit validation and clean fifteen-skill distribution audit without rerunning equivalent producers.
- [x] 7.4 Confirm all required models, tests, OpenSpec checks, evidence gates, prompt budgets, and release-risk rows are current.

## 8. Version, installation, Git, and release

- [x] 8.1 Bump all package, project, rendered guidance, distribution, and release identities to `0.63.0`.
- [x] 8.2 Build and install the local Python package, transactionally install all FlowGuard consumer skills, and verify import/source/version/content parity.
- [x] 8.3 Reconcile the authoritative repository with the user's other local FlowGuard checkout without overwriting concurrent AI changes.
- [x] 8.4 Historical disposition: immutable `v0.63.0` publication already exists; do not rerun or move its tag.
- [x] 8.5 Historical disposition: current parity belongs to v0.65.0 and MUST NOT be projected backward onto the immutable v0.63.0 release.
- [x] 8.6 Historical disposition: the current v0.65.0 release owner performs one mistake-first predictive-KB postflight for the combined lesson instead of duplicating an obsolete writer.
