## 1. Freeze the v0.68.7 baseline

- [ ] 1.1 Record the current source commit, branch, tag, GitHub Release target, package version, schema, model generation, snapshot, accepted revision, activation, installation projection, and final v0.68.7 parent receipt.
- [ ] 1.2 Reconcile the local `release/CURRENT.json` published pointer using only the existing v0.68.7 parent receipt and a publication-identity check; do not rerun product tests.
- [ ] 1.3 Re-read repository status and coordination state immediately before each later write; list any peer-owned paths and exclude them from this change.

## 2. Complete the OpenSpec contract

- [ ] 2.1 Validate the proposal capabilities and delta-spec headers for `flowguard-portable-dna`, `software-blueprint-readiness`, `target-system-blueprint`, `model-test-alignment`, `architecture-reduction`, `development-process-flow`, and `flowguard-skill-suite-distribution`.
- [ ] 2.2 Implement the portable bundle, readiness separation, compact projection, exact coverage ownership, proof-backed contraction, provider-neutral adapter, affected-only workflow, and one-final-gate requirements in the change specs.
- [ ] 2.3 Keep independent reconstruction explicitly out of scope and ensure no task creates a reconstruction runner, reconstruction receipt, fallback reader, alias, or second DNA authority.
- [ ] 2.4 Run change-local OpenSpec validation and fix only specification errors before touching production code.

## 3. Model and ownership preflight

- [ ] 3.1 Run the real FlowGuard existing-model preflight from the current observed head and accepted ModelRevisionSet; record the exact affected model, intent, owner, path-quality, and topology fingerprints.
- [ ] 3.2 Freeze an affected owner table covering portable export, readiness, compact projection, model-test alignment, architecture reduction, provider adapters, DPF, and each changed skill.
- [ ] 3.3 Confirm that every changed behavior has one current owner and that unchanged models and receipts are eligible only for exact reuse.
- [ ] 3.4 Create the implementation observation/model handoff required by ModelMesh and DevelopmentProcessFlow; do not make a candidate or path-quality result a contraction authorization.

## 4. Portable blueprint and readiness implementation

- [ ] 4.1 Trace the existing `implementation_blueprint`, `canonical_blueprint_projection`, `blueprint_compact_projection`, `target_system_blueprint`, `project_blueprint`, and `affected_blueprint_reader` paths and select the single current export owner.
- [ ] 4.2 Add the portable bundle manifest/shard projection using the existing canonical layer identities; include model hierarchy, interfaces, input/state/output contracts, intent, resources, implementation bindings, test/oracle bindings, provider identity, and readiness fingerprints.
- [ ] 4.3 Add isolated bundle verification that recomputes content identity, resolves every shard and parent-child reference, rejects stale/foreign/duplicate members, and never falls back to source or another report.
- [ ] 4.4 Add separate static, portable, and execution status fields with explicit `not_run`/incomplete claim boundaries; do not add a reconstruction status.
- [ ] 4.5 Add compact summary, candidate-detail-by-id, and explicit full projections that preserve the complete denominator, fingerprints, unresolved ids, and skipped/not-run statuses.
- [ ] 4.6 Add focused positive, missing-shard, changed-shard, stale-provider, stale-model, and compact-integrity tests.

## 5. Model-Test Alignment and evidence ownership

- [ ] 5.1 Inventory current behavior blocks, code contracts, test/native members, checker designs, oracles, execution owners, and existing receipts from one frozen observation.
- [ ] 5.2 Define and persist explicit coverage sets that map one current receipt to every behavior block, case, and coverage edge it actually covers.
- [ ] 5.3 Keep static checker design, terminal execution, parent aggregation, and `not_run` separate; reject parent-receipt fan-out without a typed coverage set.
- [ ] 5.4 Close affected public-entry, state-write, side-effect, error-recovery, model-activation, and release-identity rows first; leave unexecuted rows visible.
- [ ] 5.5 Add focused tests for missing coverage, stale receipt, duplicate coverage owner, cross-owner coverage, skipped evidence, and exact reuse.

## 6. Provider-neutral adapter coverage

- [ ] 6.1 Confirm the core export and readiness path accepts software and non-code workflow profiles without Python-specific fields.
- [ ] 6.2 Define the provider adapter contract for provider identity, target revision, observations, actors/inputs/states/transitions/outputs, resources, intent, verification, findings, and claim boundary.
- [ ] 6.3 Add fail-closed behavior for missing or stale adapters; do not substitute Python, synthetic, or alternate providers.
- [ ] 6.4 Run the existing TypeScript and non-code workflow focused fixtures and add one real adapter conformance fixture for each profile without implementing a reconstruction path.

## 7. Architecture reduction and path contraction

- [ ] 7.1 Run the current self-architecture inventory once after functional changes stabilize and capture the complete candidate denominator and reverse-caller index.
- [ ] 7.2 Repair caller classification for functions, methods, properties, dunders, serializers, fingerprints, CLI runners, model runners, manifest references, blueprint references, registries, and string/reflective calls.
- [ ] 7.3 Evaluate duplicate route, duplicate branch, fallback/alias, and validation candidates with complete caller, consumer, contract, state, error, side-effect, public-surface, model, and test proof.
- [ ] 7.4 Rebind and parity-check the six identical `.flowguard/*/run_checks.py` wrappers against their direct `model.py` runners; remove them only if all blueprint, commitment, documentation, and test references are current.
- [ ] 7.5 Consolidate the three strict duplicate-key JSON reader implementations under one domain owner while preserving malformed, duplicate-key, non-finite, root-type, exception-type, and error-context behavior.
- [ ] 7.6 Evaluate only small pure-helper families with exact input/output/order/empty/error/state/side-effect equivalence; do not create a generic utility sink.
- [ ] 7.7 Retain or mark unresolved all dynamic entries, serializers, fingerprints, public facades, adapters, broad serialization paths, and oversized modules lacking model-derived target structure.
- [ ] 7.8 Run StructureMesh for the largest responsibility clusters and split only when a named model supplies single owners, interfaces, facade boundaries, dependency/config ownership, and parity evidence.
- [ ] 7.9 Recompute the compact reduction report and verify candidate count, unresolved ids, retained responsibilities, and post-action owners without shrinking the denominator.

## 8. Prompt and skill maintenance

- [ ] 8.1 Update only the affected author sources for `flowguard`, `flowguard-existing-model-preflight`, `flowguard-model-mesh`, `flowguard-model-test-alignment`, `flowguard-architecture-reduction`, `flowguard-structure-mesh`, `flowguard-test-mesh`, and `flowguard-development-process-flow`.
- [ ] 8.2 State the static/portable/execution distinction, provider-neutral routing, compact/detail/full read modes, proof-backed contraction, and one-final-gate order in the affected prompt/reference contracts.
- [ ] 8.3 Remove duplicated wording only when hard gates, no-fallback rules, claim boundaries, and owner responsibilities remain intact.
- [ ] 8.4 Run affected SkillGuard source/contract/native checks and prompt-budget checks; do not synchronize the installation while the author source is still changing.
- [ ] 8.5 Rebuild the complete author-unit projection once all skill edits are stable and verify that consumer projections contain no private SkillGuard authority.

## 9. Affected-only verification and one cleanup closure

- [ ] 9.1 Run focused tests for every changed code/module path and affected positive, boundary, and negative case.
- [ ] 9.2 Run affected model regressions, parent/child topology closure, Model-Test Alignment, TestMesh receipt reuse, and OpenSpec change-local validation.
- [ ] 9.3 Do not run full pytest, all-model full regressions, all-skill native checks, installation parity, or full self-maintenance while the implementation scope is still changing.
- [ ] 9.4 Perform one consolidated ArchitectureReduction/StructureMesh cleanup pass after all functional behavior is stable; resolve every authorized action or keep its blocker visible.
- [ ] 9.5 Re-run only the cleanup-affected checks and regenerate the current compact self-blueprint/reduction projections.

## 10. Freeze version, OpenSpec, model, and installation identities

- [ ] 10.1 Change the current package and release-facing records from 0.68.7 to 0.68.8 exactly once; leave historical archives, snapshots, revisions, activation records, and ignored build/evidence outputs unchanged.
- [ ] 10.2 Update the v0.68.8 changelog/release notes and document the portable DNA and cleanup claim boundaries without claiming independent reconstruction.
- [ ] 10.3 Verify and archive the OpenSpec change, sync delta requirements to main specs, and confirm active changes return to zero.
- [ ] 10.4 Re-observe the final source and resource inventory, build one accepted ModelRevisionSet, update affected models/intents/owners, and activate it atomically.
- [ ] 10.5 Compile the complete SkillGuard author unit, synchronize author shadow and clean consumer projection, install the editable 0.68.8 package, and run formal/shadow/installed parity.
- [ ] 10.6 Materialize the final self-blueprint portable bundle and verify it in an isolated directory; bind the export identity to the final source/model/version identities.

## 11. Final release gate

- [ ] 11.1 Inspect peer-agent changes again, review staged ownership and privacy boundaries, and ensure evidence/build/cache/work artifacts are not staged.
- [ ] 11.2 Run full validation plan-only and resolve every `blocked` owner before starting a producer.
- [ ] 11.3 Start exactly one supervised foreground v0.68.8 full release parent; let it execute only stale/missing owners and reuse exact current receipts for the rest.
- [ ] 11.4 Confirm the parent reaches 10/10 pass, zero failure/blocker/release-only not-run, complete receipts/manifests, and zero descendant processes.
- [ ] 11.5 If a final owner fails, fix only its root cause, confirm descendant cleanup, re-run plan-only, and reuse unaffected v0.68.8 receipts; do not launch a second manual full suite.

## 12. Commit, publish, and close

- [ ] 12.1 Bind the passed parent receipt to the local release candidate and make no further source, model, installation, or staging changes.
- [ ] 12.2 Commit the owned v0.68.8 changes, push `main`, and verify the remote commit.
- [ ] 12.3 Create and push an immutable annotated `v0.68.8` tag targeting that exact commit.
- [ ] 12.4 Create the source-only GitHub Release with zero attachments, non-draft/non-prerelease status, and claims limited to current evidence.
- [ ] 12.5 Run tag and published identity checks comparing HEAD, origin/main, tag, GitHub Release, asset policy, and final parent receipt without rerunning product tests.
- [ ] 12.6 Advance local release evidence `CURRENT` to v0.68.8, verify installed version/projection parity, and confirm the final worktree is clean.

## 13. Predictive KB postflight

- [ ] 13.1 Check for mistake-first signals, especially scope changes, receipt reuse, peer preservation, prompt/skill routing, and the decision to remove reconstruction from the release.
- [ ] 13.2 If a reusable lesson or route gap exists, append exactly one structured KB observation with the final receipt; if the user’s scope still forbids KB writes, record the postflight as checked and leave the KB unchanged.
