## 1. Freeze and reconcile the current release

- [x] 1.1 Record the current branch, HEAD, origin, tag `v0.68.9`, installed
  package version, project adoption version, and active model pointer.
- [x] 1.2 Preserve unrelated parallel-agent changes and restrict edits to the
  declared v0.68.9 correction scope.
- [x] 1.3 Run project adoption upgrade and confirm project records all say
  `0.68.9` before model revision work begins.

## 2. Repair the authoritative model pointer

- [x] 2.1 Generate the read-only live model revision plan and freeze its
  candidate snapshot fingerprint and affected closure.
- [x] 2.2 Execute the six required native owner routes exactly once under
  their declared owners: behavior commitment ledger, field lifecycle mesh,
  model mesh maintenance, model-test alignment, and test mesh maintenance.
- [x] 2.3 Verify each owner receipt has current inputs, one owner, terminal
  status, and explicit `not_run`/gap detail where applicable.
- [x] 2.4 Build one candidate ModelRevisionSet with the live snapshot,
  transition predecessors, contribution/disposition inputs, and owner
  evidence; reject incomplete or copied receipts.
- [x] 2.5 Activate the candidate atomically and re-run model-system audit;
  project authority must point to the accepted snapshot and activation receipt.

## 3. Make the model directory the practical DNA

- [x] 3.1 Add a directory-first portable export helper that reuses the
  canonical projection writer and returns manifest, tree, projection, and
  source-blueprint fingerprints.
- [x] 3.2 Add directory verification that rejects unknown files, duplicate
  shards, path escapes, stale fingerprints, duplicate JSON keys, and non-finite
  values while preserving typed evidence statuses.
- [x] 3.3 Add CLI routes for explicit directory export and directory verify;
  ordinary self/project modeling must not call the monolithic bundle writer.
- [x] 3.4 Keep the existing single-file bundle as an explicit derived option;
  include the source directory fingerprint and do not make it an authority.
- [x] 3.5 Add focused tests for exact round trip, tamper detection, omitted
  not-run evidence, and no source/reconstruction execution.

## 4. Close model, code, and test bindings

- [x] 4.1 Compare every affected model obligation with one owner code contract,
  implementation surface, semantic rule, oracle, exact test/native member, and
  execution owner.
- [x] 4.2 Emit a bidirectional coverage report: no required obligation without
  a binding and no behavior-bearing surface without a model or explicit
  terminal disposition.
- [x] 4.3 Preserve planned/static design separately from current execution;
  never promote `not_run`, stale, skipped, or blocked evidence.
- [x] 4.4 Add focused tests for binding referential integrity, owner identity,
  current fingerprints, and wrong-parent/foreign-receipt rejection.

## 5. Reduce routes and payload cost with proof

- [x] 5.1 Run the self architecture-reduction review against the repaired
  blueprint and freeze its independent candidate denominator.
- [x] 5.2 For each candidate choose retain, merge, delegate, remove,
  explicit-on-demand, or unresolved using current observable contract proof;
  do not decide from age, similarity, or token cost alone.
- [x] 5.3 Contract or remove only proven duplicate wrappers and unowned
  historical handoffs; keep unresolved candidates visible.
- [x] 5.4 Ensure prompt material is catalogued once, preselected by exact
  trigger, and expanded only when the route requires it.
- [x] 5.5 Add focused route-parity and no-fallback tests; do not add aliases or
  compatibility readers.

## 6. Validate, install, and finalize v0.68.9

- [x] 6.1 Run affected model regressions and focused skill/portable/alignment
  checks once after source changes stabilize; retain their immutable receipts.
- [x] 6.2 Run SkillGuard maintainer audit and rebuild the clean consumer
  projection for the current author unit.
- [x] 6.3 Synchronize the local installation only from the frozen installation
  projection and verify installed/source parity.
- [x] 6.4 Freeze source, model, toolchain, install, and release identities.
- [x] 6.5 Run exactly one foreground full validation parent; inspect all child
  receipts and refuse closure on blocked, skipped, stale, or `not_run` owners.
- [x] 6.6 Validate OpenSpec artifacts and mark this corrective change complete;
  archive only after the terminal evidence is truthful.
- [x] 6.7 Commit only owned changes, update the existing `v0.68.9` tag/release
  to the final commit, push to GitHub, and verify remote identity without
  creating `v0.68.10`.
