## 1. Baseline, Coordination, And FlowGuard Plan

- [x] 1.1 Record current FlowGuard package/schema/project-audit evidence and run full Existing Model Preflight over BCL, preflight, similarity, miss, process, field, contract, alignment, and test owners.
- [x] 1.2 Inventory current peer-written OpenSpec changes and recent shared-file writes; preserve them and define fresh-read merge boundaries for overlapping modules/specs.
- [x] 1.3 Compile the implementation into PlanDetail rows and run PlanDetail plus AgentWorkflowRehearsal with OpenSpec, FlowGuard satellites, SkillGuard, installation, single-owner checks, failure branches, and scoped pre-implementation claim.
- [x] 1.4 Build FieldLifecycleMesh rows and Code Structure Recommendation ownership for every added/replaced field, module, public API, side effect, adapter, and validation boundary.

## 2. Behavior Commitment Schema And Review

- [x] 2.1 Add behavior-plane, actor-kind, relation-type, lookup-status, and relation-role constants with stable serialization values.
- [x] 2.2 Add `BehaviorLookupBinding` and `BehaviorCommitmentRelation` dataclasses and extend `BehaviorCommitment` with plane, actor kind, typed relations, and lookup binding.
- [x] 2.3 Replace runtime dependency review with registered-target, relation-matrix, and cross-plane-rationale checks; reject the former dependency field at the current boundary.
- [x] 2.4 Add plane/actor/relation/lookup/former-shape axes and stable case projections to BCL ContractExhaustion planning and review output.
- [x] 2.5 Add canonical ledger mapping load/serialize/fingerprint helpers and public exports without creating a route id.

## 3. Canonical Project Ledger And Direct Replacement

- [x] 3.1 Add the canonical `.flowguard/behavior_commitment_ledger/ledger.json` for the current project with classified commitments, actors, relations, lookup bindings, source surfaces, and expected ids.
- [x] 3.2 Convert the project BCL `model.py` into a thin canonical-ledger loader and keep `run_checks.py` result output evidence-only.
- [x] 3.3 Update BCL public templates/template text to generate `ledger.json`, loader adapter, checks, and notes with exactly one active authority.
- [x] 3.4 Remove artifact-upgrade/project-upgrade conversion paths and require direct current ledger authorship.
- [x] 3.5 Prove former Python/JSON shapes and old dependency fields are rejected, current semantic coverage is complete, and the embedded inventory is retired.

## 4. Plane-First Lookup And Existing Model Preflight

- [x] 4.1 Create `behavior_commitment_lookup.py` with query, hit, report, normalization, plane-first filtering, deterministic scoring, relation traversal, and ledger fingerprint behavior.
- [x] 4.2 Add exact id/tool/error/path and structured-term matching with bounded `top_k`, explainable reasons, and ambiguity/blocked statuses.
- [x] 4.3 Integrate lookup into `existing_model_preflight_from_project()` before path discovery and project primary commitment owner models into model hits.
- [x] 4.4 Extend ExistingModelPreflight/report serialization and review with lookup status, primary plane, primary/related hits, ambiguity, fingerprint, missing/stale blocking, and unsafe cross-plane findings.
- [x] 4.5 Add the read-only `behavior-commitment-query` CLI and public API registry membership through existing BCL/preflight ownership.

## 5. Similarity, Model Miss, And Existing Process Owners

- [x] 5.1 Extend ModelSignature, similarity relation evidence, and handoffs with behavior plane; quarantine cross-plane shared-language false friends and preserve typed relations without merging owners.
- [x] 5.2 Extend UI/runtime miss records, compact miss plans, and recurring defect-family rows with affected plane, commitment, owner model, related relations, and evidence-bound error signatures.
- [x] 5.3 Implement same-plane-first Model Miss backfeed, existing-commitment reuse, typed multi-plane context, and coverage-gap creation only when no promise exists.
- [x] 5.4 Update PlanDetail/AgentWorkflow/DevelopmentProcess projections and guidance so existing step receipts, continue/rework gates, peer-write invalidation, and process-tree safety remain authoritative without copying product behavior into AI steps.

## 6. Project Models, Contract Exhaustion, And Alignment

- [x] 6.1 Update existing BCL, preflight, similarity, model-miss, lightweight-miss, development-process, and self-maintenance models with plane-aware correct/broken cases and no new route profile.
- [x] 6.2 Add finite plane-kind-actor, cross-plane relation, lookup, former-shape rejection, and miss-backfeed ContractExhaustion cases with actionable oracles and stable downstream ids.
- [x] 6.3 Add FieldLifecycleMesh evidence rows/projections/dispositions for all new fields and the removed dependency field.
- [x] 6.4 Add Model-Test Alignment obligations, public CodeContracts, closure targets, and current test bindings for schema, lookup, preflight, similarity, former-shape rejection, and miss backfeed.
- [x] 6.5 Update self-maintenance child evidence and risk/claim boundaries so local model green cannot hide stale or missing plane-upgrade evidence.

## 7. Rules, Skills, Templates, And Documentation

- [x] 7.1 Update the canonical project-rule generator with `behavior.plane_partitioning`, revise BCL/preflight/miss/process wording, regenerate AGENTS guidance, and preserve peer-managed rules.
- [x] 7.2 Update BCL, ExistingModelPreflight, ModelMiss, DevelopmentProcessFlow, AgentWorkflowRehearsal, PlanDetailing, and model-first SKILL guidance and directly required protocols.
- [x] 7.3 Update affected `agents/openai.yaml` prompts so plane classification, primary/related separation, same-plane backfeed, and route-native authority are visible to Codex.
- [x] 7.4 Update each affected `.skillguard/contract-source.json`, regenerate compiled contracts/check manifests, and do not hand-edit generated authority.
- [x] 7.5 Update public docs, command examples, API docs, BCL/preflight/model-miss/process notes, version/change notes, and non-guarantee wording.

## 8. Tests And Focused Validation

- [x] 8.1 Add `tests/test_behavior_commitment_lookup.py` for deterministic scoring, wrong-plane exclusion, typed related hits, ambiguity, blocked lookup, fingerprint, and CLI/API parity.
- [x] 8.2 Add `tests/test_behavior_plane_upgrade.py` for the UI port-bridge scenario, product proxy scenario, three-plane `download`, same-plane miss reuse, typed multi-plane context, and no new route id.
- [x] 8.3 Update BCL, preflight, similarity, recurring miss, former-shape rejection, field lifecycle, ContractExhaustion, Model-Test Alignment, API, template, documentation, process, and public-surface tests.
- [x] 8.4 Update skill trigger/docs/prompt-parity/contract-compiler/installer/installed-layout tests and verify canonical machine values remain language-neutral.
- [x] 8.5 Run all focused verification-contract test commands, classify each failure through DevelopmentProcessFlow, fix root causes, and rerun to current green.
- [x] 8.6 Run affected registered model regressions and consume final receipts for BCL, preflight, similarity, miss, process, and self-maintenance models.

## 9. Installation, Full Validation, And Completion

- [x] 9.1 Install the complete skill suite to an isolated shadow target; run installer check/parity, static suite checks, markers, SkillGuard project audit, and repair every mismatch.
- [x] 9.2 Re-read recent peer writes, merge compatible changes, rerun minimum affected validation, and preserve any newer package/schema/version state.
- [x] 9.3 Freeze functional source/tool identities, reuse current owner receipts, and let one explicit owner run only missing affected checks followed by one final parent TestMesh; never schedule, resume, or background-retry it.
- [x] 9.4 Run FlowGuard project audit and strict OpenSpec validation; OpenSpec must consume the final parent receipt read-only and must not execute FlowGuard checks.
- [x] 9.5 Perform a requirement-by-requirement completion audit against current files and evidence, record KB postflight, and leave external publication/Git operations explicitly out of scope.
