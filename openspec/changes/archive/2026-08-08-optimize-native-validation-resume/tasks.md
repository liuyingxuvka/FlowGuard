## 1. Freeze native ownership

- [x] 1.1 Split the DevelopmentProcessFlow native contract into distinct lifecycle, strategy-equivalence, atomic author-sync, bounded validation-observation, plan-detailing, and agent-workflow bindings.
- [x] 1.2 Map every required obligation and every declared implementation input to one exact native binding and compile the current SkillGuard contract trio.
- [x] 1.3 Confirm the contracted DevelopmentProcessFlow owner no longer duplicates sibling-owned test files without an independent obligation.

## 2. Complete native receipt currentness

- [x] 2.1 Resolve and snapshot every declared native command path and exact path selector, not only the first command.
- [x] 2.2 Include native receipt producer sources and all contract, manifest, suite, obligation, toolchain, environment, proof, and result identities in current verification.
- [x] 2.3 Reject reuse when the current artifact-id set differs from the receipt-bound artifact-id set.

## 3. Add exact-current native resume

- [x] 3.1 Add explicit `--resume` execution semantics that accept only independently verified terminal-pass full member receipts.
- [x] 3.2 Execute every selected missing or stale member once under its declared owner and preserve visible non-pass results.
- [x] 3.3 Report requested, passed, executed, and reused member counts plus each member's disposition and receipt identity.
- [x] 3.4 Make the frozen parent full-validation command invoke the native child through `--resume`.

## 4. Retire the obsolete synchronization test path

- [x] 4.1 Remove tests that import or patch the deleted whole-workspace shadow synchronization script.
- [x] 4.2 Verify that all still-required author projection behavior remains covered by the current atomic installer-owned author-sync test owner.
- [x] 4.3 Confirm no compatibility module, alternate synchronizer, or duplicate author projection authority remains.

## 5. Focused verification and lifecycle closure

- [x] 5.1 Run contract compilation checks and focused native-receipt, resume, parent-composition, author-sync, and maintenance-script tests.
- [x] 5.2 Execute the DevelopmentProcessFlow native member once, then repeat the identical resume command and prove zero re-executed members on the second run.
- [x] 5.3 Synchronize the delta specifications to the main OpenSpec specifications and strictly validate the change.
- [x] 5.4 Archive the verified OpenSpec change before freezing final model and release identities.
