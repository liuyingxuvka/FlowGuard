## 1. Freeze Current Authority and Known-Bad Cases

- [x] 1.1 Record the exact source, observed-model, installed-package, installed-skill, Git, tag, and GitHub release identities without changing peer worktrees.
- [x] 1.2 Add focused known-bad tests for omitted task facts, conflicting route ownership, duplicate owner resolutions, caller-forged proof currentness, direct-user-choice overclaim, raw-count mesh activation, and read-only status side effects.
- [x] 1.3 Add pre-code ModelTestAlignment obligations and oracles for every new or changed external behavior and mark execution evidence not-run.

## 2. Canonical Facts, Routes, and Owner Results

- [x] 2.1 Extend task facts with source-plane provenance and declared, unknown, omitted, scoped-out, contradictory, and unmapped dispositions.
- [x] 2.2 Compile the minimum fact denominator independently and create explicit demand or blocking diagnostics for every unresolved fact.
- [x] 2.3 Define one dependency-light canonical public-owner descriptor and derive route admission and task-coverage projections from it.
- [x] 2.4 Reject retired or conflicting route identities without aliases, fallback mappings, or runtime reconciliation.
- [x] 2.5 Define one immutable owner-resolution value and make task-coverage and maturation views consume the same identity and fingerprint.
- [x] 2.6 Require proof artifacts used as current evidence to carry and validate their command, result path, timestamp, subject, and fingerprint instead of trusting caller-set booleans.
- [x] 2.7 Bind each independently inspected request, current-model, public-surface, and lifecycle plane to one current source snapshot; a missing plane must remain a blocking denominator gap.
- [x] 2.8 Add the standard ExistingModelPreflight-to-task-facts and owner-resolution projection over one current native proof artifact so AI no longer hand-transcribes ownership and surface findings.

## 3. Read-Only Understanding Status

- [x] 3.1 Implement a pure status composer that reports understanding sufficiency, user execution choice, and FlowGuard implementation admission independently.
- [x] 3.2 Add deterministic serialization and exact-identity stale, blocked, unresolved, scoped, and not-run diagnostics.
- [x] 3.3 Add the read-only model-understanding status CLI over explicit artifact paths with no execution, resume, publication, or filesystem-write behavior.
- [x] 3.4 Register the status API under the existing kernel route and add API/import/CLI parity tests without adding a new route or skill.

## 4. Semantic Whole-Flow Self Model

- [x] 4.1 Replace raw model-count activation with semantic topology triggers and add transition/regression tests.
- [x] 4.2 Classify every current observed model as connected, intentional-leaf, delegated-or-supporting, or scoped-out with rationale and required current relations.
- [x] 4.3 Add the parent semantic mesh, affected-neighbor coverage, BehaviorCommitmentLedger coverage, and gap accounting for the complete current model universe.
- [x] 4.4 Run the exact whole-FlowGuard self-understanding task through demand, one resolution per owner, maturation, receipt publication, independent receipt verification, admission, risk, and closure.
- [x] 4.5 Publish a new observed ModelRevisionSet only after the complete semantic mesh and its current consumers verify.
- [x] 4.6 Align the executable task-demand, maturation, mesh-closure, and development-process models with the runtime identities and remove fixed-count or caller-summary shortcuts.

## 5. Behavior-Preserving Contraction

- [x] 5.1 Create and validate the separate `reduce-flowguard-understanding-plumbing` OpenSpec change for behavior-preserving contraction.
- [x] 5.2 Produce CodeStructureRecommendation, ArchitectureReduction, and StructureMesh evidence for route projections, owner-result projections, thin Closure, and removal of raw-count residues.
- [x] 5.3 Remove duplicate route and owner-evidence entry paths only after facade and parity tests prove the canonical path.
- [x] 5.4 Make Closure consume upstream admission/risk/receipt decisions without route-specific re-scoring, preserving supported external behavior.
- [x] 5.5 Complete FieldLifecycle dispositions for every changed or removed public field, signature, serialized key, import, and CLI surface.

## 6. Skills, Contracts, Documentation, and Version

- [x] 6.1 Update the FlowGuard kernel, ExistingModelPreflight, and ModelMesh prompts with the independent-depth and semantic-whole-flow path.
- [x] 6.2 Update BehaviorCommitmentLedger, ModelTestAlignment, DevelopmentProcessFlow, and CodeStructureRecommendation handoffs and claim boundaries.
- [x] 6.3 Refresh only affected SkillGuard contracts and checks from the canonical source, with no direct edits to installed consumer copies.
- [x] 6.4 Update API inventory, command documentation, architecture/model documentation, README, changelog, and version metadata for patch release `0.68.5`.

## 7. Focused and Affected Validation

- [x] 7.1 Run focused tests for task facts, route topology, maturation, receipt verification, status API/CLI, mesh semantics, process admission, risk, closure, and public registry parity.
- [x] 7.2 Run affected model scenario, conformance replay, loop/stuck, progress/fairness, contract/refinement, canonical-bad-case, BCL, ModelTestAlignment, and semantic mesh checks.
- [x] 7.3 Run strict OpenSpec validation and verify both changes against their implementation and current model authority.
- [x] 7.4 Fix every affected failure, refresh only invalidated evidence owners, and confirm no peer-owned path was overwritten or rolled back.
- [x] 7.5 Preserve model-regression parent evidence on Windows worktrees whose content-addressed object paths exceed the legacy limit.

## 8. OpenSpec Closure Handoff

- [x] 8.1 Recheck current observed-model, semantic-mesh, SkillGuard-contract, and implementation-blueprint identities and confirm the implementation change is archive-ready.
- [x] 8.2 Sync verified deltas and hand the archived source tree to the separate DevelopmentProcessFlow release plan.

Installation, the unique frozen release gate, Git/GitHub publication, and the
predictive-KB postflight are release-process obligations executed after this
implementation change is archived.
