## 1. Prompt Loading Authority

- [x] 1.1 Extend prompt-bundle telemetry to derive unconditional references from selected skill shells, validate conditional reference declarations, and report explicit load-graph drift.
- [x] 1.2 Add measured byte headroom and headroom-ratio gates while renaming the bytes/3 value as a source-size regression proxy rather than provider token usage.
- [x] 1.3 Update the kernel bundle declaration and tests so only `route_index.md` is guaranteed before selection and `modeling_protocol.md` is conditional on the kernel route.

## 2. Route Admission Profiles

- [x] 2.1 Extend the existing `RouteProfile` with positive/forbidden condition ids, first action, reference edges, deepening triggers, and claim boundary; preserve serialization and topology parity.
- [x] 2.2 Add deterministic exactly-one route admission review over structured task facts with explicit no-match, forbidden, and conflict results.
- [x] 2.3 Populate all fifteen public profiles and strengthen route-profile/topology tests for complete, uniquely owned admission metadata.
- [x] 2.4 Extend the existing AI trigger model with Architecture Reduction, Behavior Commitment Ledger, Contract Exhaustion Mesh, Field Lifecycle Mesh, and Model Topology Hazard Review positive, near-neighbor, forbidden, and conflict cases.

## 3. Existing Owner Reconciliation

- [x] 3.1 Reuse one exact current owner identity index for lookup selection and final primary-owner projection validation across logical ids, paths, and fingerprints.
- [x] 3.2 Add regressions for path-to-logical-id equivalence, exact fingerprint, absolute/relative path forms, similar basenames, wrong fingerprints, and ambiguous identity.

## 4. Narrow User-Facing Entry

- [x] 4.1 Rewrite the kernel `SKILL.md`, route index, and `openai.yaml` as a narrow route shell with conditional deepening references and no loss of native closure requirements.
- [x] 4.2 Shrink the generated project AGENTS block through `docs/agents_snippet.md` and `flowguard/project_adoption.py`, preserving only universal current-authority, routing, process, and no-fake-adoption invariants.
- [x] 4.3 Add documentation/projection tests proving each removed rule remains owned by one reachable reference and no unconditional deep reference is hidden.

## 5. Model and Maintenance Evidence

- [x] 5.1 Update existing guidance-compression, self-maintenance, minimum-entry, and Existing Model Preflight model inputs/checks; do not create a new route model.
- [x] 5.2 Refresh target-owned SkillGuard contract source, compiled contract, check manifest, and affected-only validation inventory through the repository generator.
- [x] 5.3 Update package version to `0.68.2` and add a concise changelog entry without installing or publishing.

## 6. Focused Verification

- [x] 6.1 Run strict OpenSpec validation and verify apply status/context.
- [x] 6.2 Run affected prompt, route, trigger-model, Existing Model Preflight, project-adoption, skill-doc, contract, and model checks; fix every in-scope failure.
- [x] 6.3 Re-audit model authority, project adoption, dirty-tree ownership, and explicit skipped final/install/release gates; leave the change fully checked and apply-complete.

## 7. Current Model-Revision Build Handoff

- [x] 7.1 Add a typed, pointer-free current-format revision builder and thin `model-revision-build` CLI that derive the live candidate, canonical diff, affected closure, native-owner evidence refs, and accepted revision from one exact-current terminal-pass full model-regression parent receipt.
- [x] 7.2 Persist candidate and revision JSON to immutable content-addressed standard or caller-selected model-mesh paths; reject stale/incomplete/foreign evidence, inexact removals, path collisions, and all compatibility or fallback inputs.
- [x] 7.3 Add API/CLI regressions, authority-model ownership, public API/command documentation, route metadata, and refreshed affected SkillGuard contracts; run only focused affected checks and leave activation, installation, final full validation, Git, and release to the parent integration phase.
