## 1. Coordination And Frozen Baseline

- [x] 1.1 Preserve the current main-worktree peer diff, record its owned paths and identities, and perform all implementation on `agent/harden-currentness-validation` until integration.
- [x] 1.2 Re-read and integrate the completed `0.64.2` reachability, consumer-authority, documentation, and current-spec work without reverting or silently replacing peer intent.
- [x] 1.3 Record the current source, package, OpenSpec, observed-model head, author-suite, installed-suite, shadow-root, Git, and GitHub identities as separate baseline domains.
- [x] 1.4 Freeze the task-specific FlowGuard failure set, current model-owner closure, affected sibling set, validation owner boundaries, and structure facades before production edits.

## 2. OpenSpec Current Truth And History

- [x] 2.1 Add `scripts/check_openspec_semantic_sync.py` with exact delta parsing, pre-archive projection, rename validation, post-archive equality, and read-only provider boundaries.
- [x] 2.2 Add the 77-row history disposition ledger with exactly 24 current, 30 retired/replaced, 12 restore, and 11 active-change merge dispositions; reject duplicate, missing, or pending rows.
- [x] 2.3 Add `tests/test_openspec_semantic_sync.py` for exact modification, rename, missing/ambiguous source, unmatched delta, pending ledger, skip-specs, and post-archive mismatch cases.
- [x] 2.4 Add `tests/test_current_spec_authority.py` for package-owned fifteen-skill authority, zero retired public entries, no fixed superseded release version, no compatibility success, and real payload proof.
- [x] 2.5 Restore the eight current field/evidence/plan-intake/risk requirements from `simplify-flowguard-field-schema` and bind their existing tests.
- [x] 2.6 Restore the two real payload execution requirements and the accompanying Model-Test Alignment blocking scenario.
- [x] 2.7 Restore consumer prohibition scan and ordinary-project zero-author-write requirements.
- [x] 2.8 Merge the eleven inventory, contract-governance, distribution, retired-entry, and project-adoption requirements into the current owner change without creating a third member list.
- [x] 2.9 Remove or rename stale current requirements that authorize seventeen/sixteen-member topology, retired public helpers, fixed `0.64.0`, or compatibility conversion.

## 3. Authoritative Model Inventory And Revision Transaction

- [x] 3.1 Restore `.flowguard/project_adoption_version_gate/model.py` and `run_checks.py` with current purpose closure, native good/bad/oracle evidence, and Git reachability.
- [x] 3.2 Change model-system inventory to report declared, materialized, required, covered, and missing ids independently; observed authority is incomplete when any declared non-excluded model or runner is absent.
- [x] 3.3 Add pure full snapshot-diff derivation for model, relation, root, coverage, gap, field, side-effect, contract, test, and owner-artifact changes.
- [x] 3.4 Add pure fixed-point affected-closure derivation over exact base/candidate typed relations and owner bindings, including only independently affected siblings.
- [x] 3.5 Upgrade ModelRevisionSet directly to the current schema and require declared diff and closure to equal independently derived results; retain old records as non-current history only.
- [x] 3.6 Bind every revision evidence reference to affected-closure fingerprint, covered affected ids, native owner, subject, obligation, candidate snapshot, toolchain, and environment.
- [x] 3.7 Require passing evidence coverage union to equal the complete independently derived affected closure.
- [x] 3.8 Rebuild and compare the live candidate inside the project lock, perform full-head CAS, re-sample before pointer update, write immutable records first, and update the sole pointer once and last.
- [x] 3.9 Represent rollback as a reverse ModelRevisionSet bound to the full expected head, originating revision and activation, exact from/to snapshots, restored live state, and restoration evidence.
- [x] 3.10 Expand model authority audit and CLI output with declared/materialized/missing ids and full canonical snapshot comparison.
- [x] 3.11 Add inventory, revision, activation-failure-injection, same-head live-drift, exact-coverage, two-candidate race, rollback-replay, and CLI exit-code tests.
- [x] 3.12 Deepen the authoritative-model-system and project-adoption-version-gate FlowGuard models with the new known-bad failures and native checks.

## 4. Receipt Identity And Real Proof

- [x] 4.1 Require child map key, loaded child id, subject, scope, obligations, fingerprint, and child verification result to match exactly.
- [x] 4.2 Derive supersession from the current receipt store rather than caller-provided latest-child hints.
- [x] 4.3 Add exact good/bad receipt-chain tests, including store-discovered supersession and tampered verification projection.
- [x] 4.4 Remove PlanDetail synthesis of ProofArtifactRef, terminal status, exit code, currentness, or producer result from declared planning status/path.
- [x] 4.5 Make PlanDetail preserve only exact independently verified receipt references; unresolved references remain blocked.
- [x] 4.6 Replace normal-runtime caller-authoritative reuse/current/match booleans with loaded receipt plus independently derived verification in TestReuse, Model-Test Alignment, and TestMesh.
- [x] 4.7 Require reused test evidence to cover exact owner obligations, partition inventory, leaf cells, shards, and payload cases without broadening its receipt.

## 5. Frozen Validation Owner Plan And Affected-Only Freshness

- [x] 5.1 Extend ValidationOwnerContract with dependency owner ids, resource keys, precise toolchain/environment selectors, timeout/termination policy, external component bindings, and WorkContext artifact roles.
- [x] 5.2 Add deterministic owner DAG validation for unknown dependency, duplicate owner, cycle, topological order, and unmapped component blockers.
- [x] 5.3 Add ValidationOwnerPlan and ValidationParentCurrent and freeze complete owner inventory, identities, DAG, manifests, environment, external roots, and release tree before the first producer.
- [x] 5.4 Make parent save accept only the frozen parent identity, independently verify all children before composition, save the parent, and immediately verify the saved parent.
- [x] 5.5 Resolve exact-current parent receipts before children so a repeated identical request executes zero producers.
- [x] 5.6 Exclude receipts, reports, logs, run ids, output paths, caches, progress, leases, model candidates, and runtime artifacts from source freshness unless an owner explicitly consumes their functional content.
- [x] 5.7 Re-read WorkContext source bytes, preserve per-artifact snapshots, enforce project-bounded source refs, and invalidate only declared artifact consumers.
- [x] 5.8 Add owner-DAG, parent-freeze, final-drift, output-exclusion, external-component, WorkContext mutation, and affected-only reuse tests.

## 6. Resource Single-Flight And Process Supervision

- [x] 6.1 Add `flowguard/process_supervision.py` with Windows Job Object and POSIX process-group containment, terminal artifact capture, and descendant enumeration.
- [x] 6.2 Change leases to owner/resource-wide mutual exclusion with execution identity stored only in payload; preflight all selected leases before any producer.
- [x] 6.3 Implement terminate, bounded grace, force kill, and exact zero-descendant confirmation for timeout, cancellation, interruption, and keyboard interrupt.
- [x] 6.4 Preserve a cleanup-unconfirmed residual lease and incident artifact; prohibit receipt publication, later-owner start, automatic retry, or lease settlement without exact episode token and zero descendants.
- [x] 6.5 Integrate the shared supervisor and per-model resource lease into model regressions; compose exact per-model receipts into a scoped or full model parent.
- [x] 6.6 Ensure a full-tier model subset remains explicitly scoped and cannot support release/full confidence.
- [x] 6.7 Add concurrency, timeout-grandchild, cancellation, cleanup failure, residual settlement, duplicate request, and scoped-subset tests.

## 7. Sole Full Validation Owner

- [x] 7.1 Refactor `check_flowguard_skill_suite.py` to build and freeze every contract, external component, owner identity, DAG, validation manifest, and release manifest before producer execution.
- [x] 7.2 Implement fixed execution order: plan, lease preflight, full-parent lease, identity recheck, topological child reuse/execute, immediate child verification, final child recheck, parent composition, parent verification, then CURRENT publication.
- [x] 7.3 Add a universal timeout and shared process supervisor to every child command, not only model runners.
- [x] 7.4 Make `--plan-only` produce zero lease, receipt, run artifact, current pointer, or producer invocation.
- [x] 7.5 Make DevelopmentProcessFlow broad done/release/archive/publish consume exactly one independently verified `validation-parent:full`; focused, self-reported, ambiguous, stale, or multiple parents block.
- [x] 7.6 Add full-composition tests for later-owner lease preflight, concurrent identical requests, exact-parent reuse, mid-run source drift, parent verification failure, timeout cleanup, output-only change, and external-shadow affected-only execution.

## 8. Independent Bypass Closure

- [x] 8.1 Add explicit complete/scoped claim scope to UI implementation validation and require complete capability, observed inventory, visible surface, content plan, run evidence, and blindspot inputs.
- [x] 8.2 Add UI tests for complete good, missing capability, missing content plan, explicit current empty plan, and scoped omission that cannot support broad confidence.
- [x] 8.3 Extend shadow verification to load package-owned consumer authority and compare exact fifteen-member file/tree identity, reserved extras, missing members, and content drift without launching validation.
- [x] 8.4 Add shadow tests for exact parity, extra retired member, missing member, content drift, and unrelated co-located skill visibility.
- [x] 8.5 Include callable code, defaults, closure values, module file, helpers, and explicit configuration in budgeted graph fingerprints; require an explicit fingerprint for uninspectable callables.
- [x] 8.6 Add same-qualified-name/different-body, helper/default/config drift, uninspectable, and unchanged exact-reuse fingerprint tests.

## 9. Skill Maintenance And Structure Reduction

- [x] 9.1 Freeze the current `unit:flowguard-suite` member/check/owner/obligation/dependency inventory and compile its current author-side contract before skill edits.
- [x] 9.2 Record every prompt bundle byte baseline, duplicated semantic fragment, self-contained consumer gate, and route-specific native-check binding.
- [x] 9.3 Reduce every touched bundle strictly below baseline without increasing ceilings, changing the fifteen-skill topology, restoring retired public helpers, weakening a hard gate, or adding fallback/alias behavior.
- [x] 9.4 Run affected SkillGuard owners and target-native prompt/route checks, then build a clean consumer projection with zero author controls or unresolved placeholders.
- [x] 9.5 Derive a StructureMesh partition for UI implementation evidence/content visibility, receipt/ownership/process supervision, and package API registry while preserving public facades.
- [x] 9.6 Extract cohesive child modules behind current import/API facades, prove no dependency cycle/config drift, and keep routine/release parity obligations current.
- [x] 9.7 Add API-surface, import-cycle, structure ownership, facade, config/default, and exact behavior parity tests.

## 10. Layered Validation And Model Revision

- [x] 10.1 Run receipt, lifecycle, ownership, process-supervision, WorkContext, reuse, proof, and PlanDetail focused tests and fix every failure.
- [x] 10.2 Run Model-Test Alignment, TestMesh, DevelopmentProcessFlow, model-regression, long-check, full-composition, UI, shadow, budgeted, distribution, and structure focused tests and fix every failure.
- [x] 10.3 Run affected FlowGuard native models for authoritative authority, project adoption, test evidence, model-impact freshness, WorkContext, model-test-code alignment, DevelopmentProcessFlow, PlanDetail, UI, distribution, and structure.
- [x] 10.4 Build the exact candidate observed snapshot, independently derive its revision diff and affected closure, bind all native owner receipts, and activate one accepted ModelRevisionSet after source/model inputs are stable.
- [x] 10.5 Run model-system audit and project audit and require zero missing model ids, zero authority gaps, and current package/schema parity.
- [x] 10.6 Run strict validation for this change and every current OpenSpec change; reconcile rather than execute stale historical release tasks.
- [x] 10.7 Archive completed changes only after task/native evidence closure, freeze pre-archive semantic projection, run official archive, and require exact post-archive semantic equality.

## 11. Installation And Frozen Full Gate

- [x] 11.1 Build and audit the clean fifteen-skill consumer distribution from the frozen author source.
- [x] 11.2 Stage and transactionally activate the local installed consumer suite; verify installed exact currentness without rerunning semantic producers.
- [x] 11.3 Define separate package, installed-consumer, and shadow-workspace authority identities and their exact synchronization/currentness commands. Operational synchronization is delegated to the post-archive DevelopmentProcessFlow release owner.
- [x] 11.4 Define the canonical full plan-only contract: every owner disposition is `execute` or independently verified `reuse_current`, with zero blockers or unknown components. The frozen release owner executes it after OpenSpec archival.
- [x] 11.5 Define the frozen release-input set: source, toolchain, OpenSpec, model head, owner DAG, validation manifest, release tree, author projection, installed projection, shadow root, and Git inputs.
- [x] 11.6 Delegate exactly one canonical full validation for the frozen parent identity to DevelopmentProcessFlow after archive; OpenSpec does not execute or own that producer.

## 12. Version, Git, Publication, And Postflight

- [x] 12.1 Select the next semantic version from the completed breaking/current-schema impact, update package, project, docs, changelog, release checklist, consumer authority, and release manifests consistently.
- [x] 12.2 Define clean-clone reconstruction of every release-required observed model input and the complete source-only tag tree as a mandatory release gate.
- [x] 12.3 Record the required non-destructive integration rule: preserve peer history as its own commit before integrating the isolated implementation.
- [x] 12.4 Define the post-integration parity bundle across local main, source package, editable/imported package, installed skills, shadow workspace, model head, OpenSpec state, and release-tree identity.
- [x] 12.5 Hand off push, immutable annotated tag creation, tag push, and the zero-asset source-only GitHub Release to the post-archive DevelopmentProcessFlow release owner.
- [x] 12.6 Hand off published identity comparison to the same release owner; it consumes the frozen full parent receipt and MUST NOT restart heavy producers.
- [x] 12.7 Hand off predictive-KB postflight and persistent-goal closure to the same release owner after published verification. OpenSpec completion does not claim those post-archive operations already ran.
