## 1. Target Topology Shape

- [x] 1.1 Add direct-current `structural_parent_id` and `cross_boundary_parent_ids` fields to the provider-neutral topology records, strict loaders, serializers, and fingerprints without a compatibility reader.
- [x] 1.2 Derive one exact structural parent for every non-root node, preserve the declared root sentinel, and fail on missing, duplicate, foreign, or ambiguous structural parents.
- [x] 1.3 Classify consumer, producer, feedback, retry, repair, shared-resource, affected-sibling, and other semantic relations as non-structural cross-boundary edges without changing structural parentage.

## 2. Mesh Reattachment and Feedback Progress

- [x] 2.1 Validate structural reachability and acyclicity over structural edges only, then build the separate bounded feedback graph from declared return/control/retry/repair relation kinds.
- [x] 2.2 Detect every reachable multi-member or self-loop feedback SCC and require an exact current progress contract plus independent terminal evidence for its blocker, repair-token change, ranking decrease, monotone rule, or finite bound.
- [x] 2.3 Require one exact-current terminal receipt for every declared in-scope child and make the full model parent consume the exact child, interface, reattachment, and progress receipt identities.
- [x] 2.4 Preserve each child receipt's producer, subject, scope, result, and fingerprint while keeping the full parent result aggregation-only.

## 3. Evidence Authority

- [x] 3.1 Separate child/progress evidence production, immutable receipt registration, parent aggregation, and readiness consumption into exact owner-bound stages.
- [x] 3.2 Reject any self-blueprint, full-parent, mesh, audit, or qualification route that generates, relabels, or registers passing current evidence consumed by the same claim.
- [x] 3.3 Make read-only registration verify an already terminal supervised receipt without executing, resuming, simulating, or retrying its producer.

## 4. Assertion Helpers, Coverage, and Readiness

- [x] 4.1 Recursively resolve delegated assertion-helper calls inside the frozen test boundary and give every nested helper and leaf an exact source-owner, lexical-qualified-name, and content-fingerprint identity.
- [x] 4.2 Block cycles, unresolved dynamic calls, stale or ambiguous helpers, and branches without assertion/native-check leaves without borrowing a passing sibling branch.
- [x] 4.3 Preserve the original coverage-contract owner through every helper edge and keep accepted planned-checker design separate from exact terminal execution evidence and `not_run` status.
- [x] 4.4 Compose structural, cross-boundary, SCC progress, child receipt, full-parent, helper, coverage-owner, and execution findings into the truthful software-blueprint readiness prefix.

## 5. Model-Miss and Authority Renewal

- [x] 5.1 Add native model counterexamples and focused regressions for structural multi-parenting, cross-boundary misclassification, unclosed feedback SCCs, stale or self-certified progress, missing child receipts, self-generated evidence, nested helper aliasing, coverage-owner substitution, and parent-result copying.
- [x] 5.2 Run the affected native owners and point tests, repair every failure, then freeze the exact child and full-parent evidence under one pre-archive execution plan.
- [x] 5.3 Build, accept, and atomically activate one pre-archive candidate model-system revision whose observed snapshot includes the corrected topology and independently owned evidence identities.
- [x] 5.4 Run the live compact FlowGuard self-blueprint audit against that exact candidate authority, require zero remaining findings from the affected topology and evidence families, and confirm this completed change is archive-ready; record the later archive path movement as a fresh governed-source invalidation owned by DevelopmentProcessFlow.

## 6. Planning Validation

- [x] 6.1 Strictly validate this change and each synchronized affected main spec, and verify that this planning pass changed no Python, tests, executable models, or manifests.

## Post-Archive Release Handoff (not OpenSpec completion tasks)

After this change and its five companion changes are archived, DevelopmentProcessFlow owns the canonical-main-spec intent refresh, final observed authority, SkillGuard and distribution closure, installation parity, the unique foreground full release validation, publication, and postflight. The pre-archive topology and evidence receipts prove this change is archive-ready, but archive path movement makes them non-final for release.
