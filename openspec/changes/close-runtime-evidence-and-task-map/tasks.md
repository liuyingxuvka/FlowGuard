## 1. Baseline and change scaffolding

- [x] 1.1 Save the current repository, package, OpenSpec, coordination, and process baseline under the fixed work root without starting the historical producer.
- [x] 1.2 Keep the three pre-existing OpenSpec changes in their actual status and validate this change strictly.

## 2. Working-artifact policy

- [x] 2.1 Classify work/staging/bytecode before governance exclusion, preserve bytes, and exclude non-authoritative artifacts from source and release identity.
- [x] 2.2 Add layout, reparse-point, unknown-source, and release-projection regression tests.

## 3. Bounded task map

- [x] 3.1 Extend the existing affected-blueprint reader and CLI with root, changed-path, task-summary, typed task_context, provenance, gaps, and read-only behavior.
- [x] 3.2 Add deterministic compact pagination that preserves blockers, denominator fingerprints, and continuation references without whole-object loading.
- [x] 3.3 Add exact task-map and no-producer tests.

## 4. Semantic identity and finite completion

- [x] 4.1 Unify semantic command normalization and exclude only registered execution-output paths from identity.
- [x] 4.2 Make terminal-ledger loading accept a fresh unclaimed plan for the same immutable identity and implement strict reuse-only behavior.
- [x] 4.3 Add regression tests for output/input identity, terminal reuse, aborted records, and no-run-directory reuse.

## 5. Native result and compact execution

- [x] 5.1 Fix compact exploration to create a fresh lazy sequence iterator per initial state and enforce native completion counts.
- [x] 5.2 Extend native case/result contracts with exact bindings, dimensions, oracle members, child IDs, and raw artifact identity; reject marker-only or aggregate-only evidence.
- [x] 5.3 Adapt affected native runners only through their existing result selectors and add strict binding/oracle tests.

## 6. Affected closure and hierarchy

- [x] 6.1 Generate affected plans from canonical component inventory and DAG, including deletion/rename edges, with one owner execution and stale-input admission checks.
- [x] 6.2 Prevent aggregate owners from relaunching declared child owners and preserve distinct structural/native/interaction claims.
- [x] 6.3 Promote only verified native subtree receipts, execute the real five-level finite fixture, and test missing-cell/join/progress and three-way-property blockers.

## 7. Miss and JUnit evidence

- [x] 7.1 Separate prepared/blocked/closed model-miss states, require finite evidence-backed closure, and reject non-boolean evidence-current values.
- [x] 7.2 Make JUnit projection strict for malformed, missing, duplicate, hidden, xfail, optional, and child-local cases; preserve exact node IDs and outcome children.

## 8. Readiness, self-maintenance, and contraction

- [x] 8.1 Add persistent completion-cycle state and one public canonical completion-readiness route using the shared planner and typed repair link.
- [x] 8.2 Add readiness, cycle-race, scope-drift, and full-plan recomputation tests.
- [x] 8.3 Apply only the named duplicate-branch/owner contractions and update route-depth documentation without widening scope.
- [x] 8.4 Persist one immutable completion-run manifest from the shared readiness planner, consume it from full validation, and block field-level invocation/plan drift before owner leases or producers.

## 9. Qualification and handoff

- [x] 9.1 Run focused tests, strict OpenSpec validation, project-layout and distribution checks; record adoption evidence and unresolved gates.
- [x] 9.2 After all inputs freeze, perform the single authorized formal qualification and same-parent reuse-only check; do not rerun after success.
- [x] 9.3 Leave external CI/provider/release tasks and remote operations explicitly not_run, perform predictive-KB postflight, and close the goal only if every core acceptance condition has evidence.

## 10. Map trust, route cost, and partitioned composition closure

- [x] 10.1 Reject caller-declared snapshot verification and bind task-map acceptance to the current authority, subject revision, head, and projection identity.
- [x] 10.2 Reuse the actual affected traversal edges and reason paths in the map projection; do not run a second topology traversal that adds unrelated siblings.
- [x] 10.3 Require root-scoped terminal evidence and a passing root report before recursive `execution_verified` is true; preserve sibling progress separately.
- [x] 10.4 Add the production partitioned-composition gate with an independently bound boundary/coupling denominator, relation materializations, interface/refinement identity, executed cells, and parent consumption. `partition_context_from_accepted_authority` now derives the denominator only from the accepted boundary contract carried by the current authority head/revision; candidate axes, groups, relations, or caller flags cannot self-authorize a full-equivalent claim. The accepted-boundary regression suite passes.
- [x] 10.5 Select `light`, `affected`, or `full` from typed operation facts before producer admission; conflicting explicit profile/operation facts block without auto-upgrade.
- [x] 10.6 Make ordinary light map reads zero-producer while retaining the fifteen-member author assurance as one internal full owner.
- [x] 10.7 Replace repeated object-store rescans with one invocation-local offset index and decode only requested objects while retaining strict malformed/duplicate/path checks.
- [x] 10.8 Add the existing map `--projection-root` path and preserve basic navigation when no deep projection is supplied; do not invent an automatic latest-pointer authority.
- [x] 10.9 Align all fifteen route documents with the shared execution contract and lazy references, including temporary-evidence retention and conditional triggers.
- [x] 10.10 Record local cost metrics and completion/reuse invariants without launching another full producer solely for measurement.
- [x] 10.11 Gate internal AgentWorkflowRehearsal by explicit workflow-risk facts; ordinary multi-tool or public-entry work remains `not_triggered`, with route/docs and regression coverage.
