## 1. Model and Contract Grounding

- [x] 1.1 Run current existing-model, architecture-reduction, and development-process model checks and record the selected owners and contraction boundary.
- [x] 1.2 Add model/spec coverage for unified simulator selection, compact evidence, current heads, lifecycle audit, quarantine, and purge hazards.

## 2. Evidence Storage and Lifecycle

- [x] 2.1 Implement deterministic content-addressed compressed stream objects with logical/storage identities and bounded diagnostics.
- [x] 2.2 Implement immutable run manifests and atomic scope-local current-head publication.
- [x] 2.3 Implement read-only evidence audit and exact garbage-collection planning.
- [x] 2.4 Implement stale-safe quarantine apply, restore, and separately authorized exact quarantine purge.
- [x] 2.5 Add unit tests for deduplication, compression determinism, head integrity, legacy visibility, stale plans, reachability, quarantine, restore, and purge containment.

## 3. Unified Simulator and Validation Contraction

- [x] 3.1 Add the manifest-backed `python -m flowguard simulator` list and execution surface with explicit selection and unmatched-selector failure.
- [x] 3.2 Convert model-regression stdout/stderr and child projections to compact evidence descriptors without changing native runner authority.
- [x] 3.3 Convert full skill-suite child and parent results to compact summaries and one complete stream object per logical payload.
- [x] 3.4 Update release verification and command-surface tests to consume only stable summary and artifact fields.

## 4. Documentation, Version, and Skill Maintenance

- [x] 4.1 Document model source versus native runner, generated evidence, installed skills, build outputs, and local worktree boundaries plus simulator/lifecycle commands.
- [x] 4.2 Advance package, project, changelog, and rendered adoption records to FlowGuard 0.59.0.
- [x] 4.3 Recompile all maintained FlowGuard skill contracts with SkillGuard 0.4.0 and update only target-declared checks/obligations affected by this change.
- [x] 4.4 Build, audit, and transactionally install the clean consumer skill projection without author evidence or SkillGuard state.

## 5. Validation and Release

- [x] 5.1 Run focused unit/model checks and strict OpenSpec validation; repair every affected failure.
- [ ] 5.2 Freeze source/toolchain/owner plan and run one full FlowGuard release validation with compact retained evidence.
- [x] 5.3 Audit evidence size/reachability, quarantine explicitly approved obsolete generated runs, and verify current/release-pinned replay before any purge.
- [ ] 5.4 Commit, push, tag, and publish FlowGuard 0.59.0, then verify local source, installed package/skills, Git, tag, GitHub release, and CI identities. Deferred by the user on 2026-07-22 because another AI began concurrent FlowGuard work; no remote mutation is authorized.
