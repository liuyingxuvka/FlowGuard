## 1. OpenSpec and route baseline

- [x] 1.1 Reconcile the active architecture-reduction task ledger with the
  current v0.68.8 evidence without fabricating historical completion.
- [x] 1.2 Sync the architecture-reduction delta requirements into the main
  specification and keep one current owner.
- [x] 1.3 Confirm this change is the only new owner for v0.68.9 DNA closure.

## 2. Prompt and process economy

- [x] 2.1 Add unique persistent-context metrics and optional enforced budgets
  to `flowguard.prompt_budget`.
- [x] 2.2 Move ArchitectureReduction's detailed protocol edge to explicit
  triggered expansion and preserve the full protocol for on-demand use.
- [x] 2.3 Add exact conditional plan-detailing and agent-workflow reference
  edges to DevelopmentProcessFlow.
- [x] 2.4 Update focused prompt/document tests for the new staged route.

## 3. Portable DNA and evidence boundary

- [x] 3.1 Materialize the current FlowGuard self portable-blueprint bundle.
- [x] 3.2 Verify the bundle in an isolated directory with the canonical
  portable verifier and record its fingerprint.
- [x] 3.3 Keep static, portable, execution, and optional reconstruction claims
  separate in the exported artifact and documentation.
- [x] 3.4 Preserve leaf `not_run`/typed gaps when aggregate receipts pass.

## 4. Focused validation and release

- [x] 4.1 Run only affected prompt, skill, portable, and model-alignment
  checks once after the edits stabilize.
- [x] 4.2 Rebuild the author SkillGuard contracts and consumer projection under
  the existing unit:flowguard-suite authority.
- [x] 4.3 Synchronize local package and clean consumer installation after the
  source/model identities are frozen.
- [x] 4.4 Run one foreground frozen final validation parent. The final
  parent completed with status `pass` and result fingerprint
  `sha256:4316a5b7028bd64cd37974baf7b951e0c8194cde0ffa3f7e84f098e9818eee31`.
- [ ] 4.5 Bump the patch version to 0.68.9, commit, push, tag, publish, and
  perform a read-only post-release identity audit.
