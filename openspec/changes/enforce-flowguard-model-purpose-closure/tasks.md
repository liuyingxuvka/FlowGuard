## 1. Current Authority And Model Contract

- [x] 1.1 Add the current model-purpose data structures, canonical fingerprints, and fail-closed validator in `flowguard/model_purpose.py`.
- [x] 1.2 Advance the existing model-regression manifest directly and make `flowguard.model_regressions` reject missing, duplicate, placeholder, stale, or disconnected purpose closures.
- [x] 1.3 Directly adopt all 59 current regression instances with task-specific purpose, finite failure ids, native good/bad case mappings, oracle owners, candidate/runner identities, and bounded current-only claims.
- [x] 1.4 Export the supported purpose-closure API and keep normal runtime free of a v1 reader, compatibility alias, optional mode, or parallel registry.

## 2. Executable Native Proof

- [x] 2.1 Add focused positive tests for one failure, several failures, shared reusable model type with different instances, exact candidate binding, and current adoption claims.
- [x] 2.2 Add focused negative tests for post-hoc/unfrozen declarations, missing good case, missing or duplicate bad mapping, placeholder prose, disconnected oracle, stale model/runner identity, duplicate instance id, and unknown fields.
- [x] 2.3 Extend manifest and regression-orchestrator tests so audit blocks before child execution and receipts expose purpose-closure identity and claim boundary.
- [x] 2.4 Run the focused purpose/manifest/orchestrator checks and repair every failure before skill maintenance begins.

## 3. Seventeen FlowGuard Skill Contracts

- [x] 3.1 Add one concise mandatory model-instance purpose loop to every FlowGuard `SKILL.md`, preserving route-specific ownership and read-only non-creation behavior.
- [x] 3.2 Update the canonical kernel modeling protocol and relevant prompt metadata so AI declares one-or-many failures before model construction and never treats a reusable model type as permanently single-purpose.
- [x] 3.3 Extend FlowGuard native skill checks and readiness checks to reject missing markers, selectable-mode wording, permanent-purpose wording, or SkillGuard-owned domain semantics.
- [x] 3.4 Regenerate all affected current SkillGuard contract-source, compiled-contract, and check-manifest projections through the installed generic SkillGuard compiler.
- [x] 3.5 Run the exact affected native checks and static suite under one frozen owner plan; fix failures without weakening target-owned checks.

## 4. Installation And Final Evidence

- [x] 4.1 Freeze the final source/toolchain/impact inventory after all functional edits and confirm only the exact affected owners are stale.
- [x] 4.2 Install the complete FlowGuard skill suite last, verify content-aware installed parity, and refresh the global SkillGuard router only because FlowGuard entrypoint contracts changed.
- [x] 4.3 Run installed readiness and capture the current installation projection without launching an extra validation owner from a read-only currentness check.
- [x] 4.4 Execute exactly one frozen final full FlowGuard validation owner and preserve its immutable parent/child evidence.
- [x] 4.5 Run `openspec verify enforce-flowguard-model-purpose-closure --mode full --json` and strict validation; repair any failed or blocked obligation and rerun only invalidated owners.

## 5. Cross-Family Closure

- [x] 5.1 Recheck that SkillGuard remains generic, all five earlier Guard-family OpenSpec receipts remain current, and FlowGuard was installed after those families.
- [x] 5.2 Recheck that `logic-writing` and `travel-story-planner` remain current, academic/deep-research/databank skills remain absent, and no `T:` mapping exists.
- [x] 5.3 Perform the explicit predictive-KB mistake-first postflight and record the reusable locale/real-manifest validation lesson.
- [x] 5.4 Close the task only after all evidence, failures, blockers, skipped checks, residual risk, and claim boundaries are reconciled.
